# 本地升级验收 · 2026-09-08

## 第四轮：十阶段连续浏览器填写与操作名称补强

时间：2026-09-08 23:19～23:35（北京时间）。浏览器使用运行版 `20260908T150953Z-710f9b0f1c66`、独立端口53836及原合成库。新项目 `NP-2026-002 / QA 连续十阶段验收 0908`（ID `project-c35b1c4e-a6f1-4761-a150-2c96e44fb914`），全部业务内容、客户意见和签名占位均明确为模拟，非真实技术或量产批准。未用接口预填本项目表单。

1. 设计人员实际新建双规格系列，发起人与负责人均为验收设计；工艺、采购、生产、试验、质量在建项时带入。随后通过项目团队加入销售，销售节点从待分配变成验收销售，共7名成员。
2. 两个R1规格分别保存出线形式、防护、绝缘及冷却参数，并分别填写I01/I02检验要求和T01/T02试验要求。企业研发来源本轮未关联订单，不把其他项目此前的订单测试算作此项目连续流程的一部分。
3. 浏览器逐字段提交SJ-01、SJ-02/SJ-04、SJ-03/SJ-10、SJ-05，分别显式完成阶段1～4。原生日期字段使用可访问日期控件设置；自动fill未触发日期输入时，服务端正确拒绝缺项，未将工具输入差异误判为业务缺陷。
4. 添加1项系列通用件，包含图纸/BOM引用、数量2及PI01检验要求。切换生产账户，分别确认两规格整机与共用件；生产进度30%→60%→90%，显式复核后阶段5完成100%。确认姓名、日期、说明与版本均在页面保留。
5. 切换试验账户，每规格上传一份当前R1型式试验记录和合成文本附件；选择规格时自动引用各自T01/T02。只有报告但未提交SJ-06时，阶段完成被明确拒绝“以下受控表单尚未提交：设计开发验证报告”，原V5/90%保留。放弃该未保存操作，补齐SJ-06全部10字段后才完成阶段6。
6. 切换质量账户，分别提交两整机及共用件检验，每条带独立附件记录；对应I01/I02/PI01自动引用，齐套进度30%→60%→90%，质量显式复核完成阶段7。
7. 切换销售账户提交SJ-07全部12字段并完成试用；切回设计负责人提交SJ-08全部13字段并完成鉴定。SJ-09记录模拟归档索引换页、不改变R1技术要求；未勾选供应商/客户通知，不伪称实际外发或签收。提交并完成归档后，页面项目状态为已完成、进度100%。
8. 只读数据库核实持久化：十阶段均completed/100%，版本依次3/4/4/3/6/7/9/3/3/3；十份受控表单均submitted/V1；2规格、7成员、1零部件、2试验、3检验、5附件、45阶段历史，项目实际完成日期2026-09-08。

浏览器在下载验收前被Mac锁屏阻断，工具要求人工解锁。未尝试绕过锁屏，也未声称本轮新项目的浏览器下载、文件内容或升级后新标签交互已验证。此前浏览器下载记录及本轮候选完整HTTP导出测试各自独立成立。锁屏时测试页保留，设计会话未能通过浏览器退出；独立服务停止后不可访问，模拟库保留，后续继续测试应重新启动并按会话状态登录。正式3011标签页未写入。

### 本轮发现与修复

- 团队加号、规格设计输出编辑及质量附件图标缺少明确名称；补齐中文可访问名称/悬停提示。零部件变更/生产确认、试验/检验下载名称包含对应对象，便于多规格下区分。
- 零部件确认弹窗原显示planned等内部状态，附件索引显示test_report等内部分类；改为中文已知值，未知历史值仍原样保留。未改变权限、下载地址、文件名、数据或版本快照。
- 新增4项真实React渲染专项，覆盖控制名称、目标上下文、只读显隐、四种生产状态、附件类别、未知值和HTML转义。首次测试夹具缺少requirementRef导致测试异常；补齐合成夹具后通过，没有放宽生产校验。
- 完整npm test通过（含构建及74项主测试、业务/事务/导出/备份/登录链）；TypeScript、范围ESLint、diff通过。封存 `20260908T154043Z-871b1614e904` 后完整production-runtime通过，包含真实本地HTTP、D1/R2、八角色、版本冲突、十阶段100%以及Excel/Word/ZIP。

### 本地更新与边界

- 备份 `backups/hengda-2026-09-08T15-42-55-641Z-Z7QGBO` 校验通过后切换上述候选。实际启动日志匹配，PID36312/running，首页200，封存60文件校验通过。
- 17张npd表的结构及全部旧字段与备份逐值一致，quick_check=ok、外键错误0。无迁移、无依赖升级、无业务补填/密码重置、无远程发布。
- 本轮证明含模拟归档索引变更的连续十阶段路径；“完全无变更项目是否应提交更改通知单”的业务路径尚未单独验收。改版后的完整重新放行、更多角色组合、办公软件视觉排版、小屏与长期运行也不能据本轮覆盖推定通过。

---

用户再次明确回复“允许”后，本机运行权限申请成功。本轮使用封存候选 `20260907T143736Z-cf3975a22f9e`，未重新编译或替换其文件。

## 隔离运行检查

- `test:motor-production-runtime` 和 `test:design-progress-runtime` 在独立真实D1运行通过：生产权限、日期、旧版本冲突、并发、事务回滚、整机/零部件齐套、设计改版重置与进度联动。
- 指定候选运行完整 `test:production-runtime`：第一次在跨站拒绝附件请求处发生ECONNRESET，测试进程已结束；没有修改断言或将其算作通过。全新隔离库复测一次，账户/鉴权、跨账号、Origin防护、偏好、成员/订单/交接、暂停恢复、附件、Word/Excel/ZIP、十阶段连续至100%、登录限速与超大请求均通过。一次成功复测不等于证明瞬时连接问题永不发生。

## 实际浏览器检查

测试地址 `http://127.0.0.1:53836/`；保留的独立合成库 `/private/tmp/hengda-browser-qa-LvSvkr`。未使用正式3011业务页面写入或真实业务附件。

1. 生产测试账户登录，进入原有合成项目，Sheet 5初始V5/0%。两种整机分别确认生产完工，显示当前R、实际日期2026-09-08、生产人员姓名、北京时间和说明，阶段依次到30%、60%。
2. R3系列通用零部件生产确认后，整机/零部件齐套3/3，阶段90%/V8；显式复核完成后100%/V9，当前节点进入开发验证，不自动将试验或质量改为合格。
3. 第一种整机从已完成改为生产受阻并填写返工原因：实际完成清空，阶段回到进行中60%/V10，另一规格与零部件保留完工。
4. 打开V10对比V9，阶段表显示100%→60%，异步领域数据加载后仅一条整机记录有差异；展开可查看原完工日期、原说明及返工后的状态/说明。原始快照字段仍以原始存储值展示，包含UTC时间及标识；不把原始字段时间误当作页面表头北京时间。
5. 点击“导出本 Sheet”和“完整 Excel”均收到浏览器下载事件。事件仅证明交付；文件内部内容和时区校验由前轮导出专项及本轮完整HTTP专项提供。
6. 生产退出后返回登录页；设计负责人登录同项目，Sheet 5整机及零部件确认按钮均为0，仍可查看生产姓名/受阻说明。退出设计账户后关闭测试标签页，停止核实的隔离服务PID68706，结束状态143；测试库保留。
7. 浏览器所采集error/warn日志为空。两次定位观察超时后重新读取状态，确认先前保存成功再继续，没有重复提交；版本比较出现加载状态后读取同一窗口直到完成，没有因观察等待而重启服务。

本轮不是完整十阶段逐字段浏览器验收，也不包含所有小屏、键盘、办公软件视觉排版、长期运行或最大容量验证。

## 本地升级和数据保护

- 升级前首页200，现用服务PID11930/running。停止保活服务后创建成套备份：`backups/hengda-2026-09-08T02-29-39-371Z-m1IWgQ`。
- 启用候选后重新启动保活服务：PID69359/running，日志确认 `20260907T143736Z-cf3975a22f9e`，首页200，封存60文件校验通过。
- 0010_plain_lake已登记执行。以备份为基准，17张npd表全部旧列/旧值逐字段一致，包含40条阶段历史和原快照；8账户、4项目、7规格及其他记录数量均保持。整机仅新增confirmed_by、confirmed_at、production_note，所有旧规格新字段均为NULL/空串，没有虚构历史确认。
- 数据库quick_check=ok，foreign_key_check=0。无原模板改动、无账号密码重置、无业务资料补填、无公网发布。

现用地址仍为 `http://localhost:3011/`。旧库表单缺项仍需管理员依据真实资料复核；软件测试不能代替业务验收或批准。

## 第二轮：历史字段可读性（2026-09-08晚）

本轮针对上一轮第4项的原始英文键名/UTC可读性问题，仅调整历史内容的显示，不改变快照、差异判定、版本生成、业务状态或导出数据。字段时间明确显示北京时间并保留秒；阶段操作元信息仍沿用既有北京时间分钟显示。无法识别的历史时间保留原值并提示，不把业务日期或自定义正文当作时间转换。已知状态中文化，未知值保留；缺失字段不虚构，历史人员仅存标识时不按当前姓名反推历史姓名。

- 新增 `RevisionValues`，受控表单按对应form_code映射中文字段标签，未知扩展字段原样保留；可展开查看未转换的字段值。该入口是语义字段原值，并非原JSON字节证明；数据库中原快照不改写。
- `revision-view` 与 `revision-values-render` 共6项通过，涵盖时区/秒/跨年/毫秒、非法日期、未知值、零/布尔/数组、表单标签、转义、原值保留、缺快照和差异语义不变；TypeScript、范围ESLint、diff检查及生产构建通过。本轮没有声称重跑完整npm test。
- 封存候选 `20260908T144833Z-05dcd3253432` 的完整production-runtime在全新独立库一次通过：八角色、登录退出/撤销/限速、跨账号与Origin拒绝、附件、交接/协作、生命周期、Word/Excel/ZIP、十阶段至100%。
- 使用原隔离库启动53836，生产测试账号只读打开Sheet 5的V10/V9比较：阶段100%→60%，仅一条电机记录有差异；中文“生产确认时间/说明”显示10:24:33→10:26:04（北京时间），原值展开仍为02:24:33及completed。截图检查左右栏标签、换行和说明可读。切回单版本展开SJ-01，立项来源、产品名称、日期、功能、风险等正文标签为中文。浏览器warn/error采集为空；退出、关闭临时标签并停止隔离PID29030，保留测试库。本轮未提交项目业务修改。
- 正式服务升级前200；停服后成套备份 `backups/hengda-2026-09-08T14-54-40-339Z-UK4cli`，备份verify通过。启用同一候选后PID29291/running，日志版本匹配，首页200，封存60文件校验通过。
- 与本次备份对比，17张npd表结构及所有字段值完全一致，quick_check=ok、foreign_key_check=0；8账户、4项目、7规格、40阶段历史、23表单均保留。以本次备份为基准的活动数为11，不与更早运行快照混淆。本轮无迁移、无依赖/原模板修改、无正式账号重置、无远程发布。

本轮证明历史展示与上述回归范围，不关闭全十阶段逐字段浏览器、全部小屏/键盘、旧库业务资料复核、依赖告警及异盘备份安排等尚未完成事项。

## 第三轮：录入弹窗防误关闭与键盘可靠性

- 现有实现风险：通用Modal的外部点击及关闭按钮直接卸载内容，多数编辑窗口保存中也能取消；没有焦点隔离或Escape处理。重用现有视觉样式，以原生dialog隔离背景，补上受控关闭、初始标题焦点、关闭后回到入口、Tab首尾循环及保存失败后焦点恢复。
- 18个当前编辑入口全部显式传入protectChanges与busy，覆盖建项、规格、受控表单、节点、零部件、试验、检验、人员/账户、客户/订单、交接、项目状态、看板配置。输入、选择和结构按钮（增加/移除规格、检验对象类型切换等）标记本次窗口有编辑操作。关闭、取消、Escape和完整遮罩点击统一确认；继续编辑保留原子树，显式放弃才卸载。只读窗口不显示未保存警告。编辑过再还原也会提示，未声称精确字段差异检测。
- 保存中将编辑区设为inert、禁用关闭与取消并显示等待信息；不取消在途请求，不自动重试，不清空失败输入。完成后原有成功处理关闭窗口，失败回到原操作控件；若控件已不存在则回到标题。仅在编辑或保存期间注册beforeunload并于卸载移除，不新增localStorage/sessionStorage或自动草稿。该机制不能保证系统强退/断电恢复，也不能保证所有浏览器都展示离开提示。
- 完整npm test在首个候选通过；随后浏览器发现两个具体焦点问题，分别修复并补测：原生dialog末尾Tab会进入浏览器栏，以及inert解除后失败焦点落在页面。最终版本7项Modal专项及17项既有渲染/任务专项共24项通过；TypeScript、范围ESLint、diff和生产构建通过。最终封存候选 `20260908T150953Z-710f9b0f1c66` 完整production-runtime一次通过（八角色/会话、Origin、附件、版本/交接、生命周期、归档及十阶段100%）。未将中间候选的整条npm test冒称最终修改后完整重跑。
- 独立53836合成库实操：设计账户打开建项，标题获焦且背景不在可交互AX树内；首尾Tab/Shift+Tab修复后循环正确。填写项目名后关闭提示确认，继续编辑原文保留，Escape仍提示，显式放弃返回创建入口。仅点增加规格再取消也会提示，不因没输入文字而漏掉结构修改。确认框默认焦点在“继续编辑”，两个选项双向循环，截图可读。
- 同一模拟项目Sheet 6新建SJ-06只填项目名，正式提交被字段校验拒绝且原文保留；取消仍需确认，继续后保存草稿成功，只形成草稿V1/阶段V5，阶段仍0%、项目49%，未伪造验证合格。最终候选再次加载草稿，填写变更原因后重复不完整提交，拒绝后焦点正确回到“提交受控版本”；放弃仅丢弃该未保存原因，已保存草稿仍在。只读查看能见草稿原文，Escape直接关闭，最终退出到登录页。
- 390×844临时视窗检查确认框布局：页面/外层宽390，卡片宽374.4、scrollWidth374，无横向溢出；截图输出经过缩放，不据此声称完整移动端视觉验收。已恢复默认视窗并关闭验收标签。采集warn/error为空。保存中关闭防护与页面离开监听有实际组件回调专项；未用浏览器网络限速或强制断网制造长时在途请求，未声称这些故障场景已全部实测。

本轮仅操作独立合成库，未使用3011真实项目做试写；原模板、数据库结构、依赖锁定、账号凭据及远程访问范围不变。

本地启用：升级前服务PID29291/running、首页200；停止后备份 `backups/hengda-2026-09-08T15-14-44-849Z-ilXtff`，verify通过。启用 `20260908T150953Z-710f9b0f1c66` 后PID32482/running，日志版本匹配、首页200、60文件封存校验通过。与本次备份比较17张npd表结构和所有字段完全一致，quick_check=ok、外键0；8账户、4项目、7规格、40历史版本、23表单保持。独立测试PID31910已停止（143），测试库保留；本轮没有迁移或公网发布。
