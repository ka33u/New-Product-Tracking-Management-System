# 独立测试库浏览器验收 · 2026-09-07

用户明确回复“允许”后，按 Sites 验收规则使用实际浏览器页面、表单、文件选择器及下载事件测试。没有操作正式 3011 页面，没有将测试资料写入正式数据库。

## 环境与边界

- 测试地址：`http://127.0.0.1:53836/`，与原业务浏览器 `http://localhost:3011/` 分离。
- 独立 D1/R2：`/private/tmp/hengda-browser-qa-LvSvkr`。仅有合成的 8 个账户、虚拟客户、1 个项目、2 种规格、1 个零部件、1 个订单和测试附件。
- 初始测试版本：`20260907T004244Z-efeaf8e9a302`；北京时间修复后使用 `20260907T110919Z-225bd30e6d4a` 重启同一独立测试库并复测。
- 测试附件：`/private/tmp/hengda-browser-qa-evidence-20260907.txt`，内容明确标为虚拟验收资料。未使用用户原始开发程序文档作为上传样本。
- 结束时退出测试账户、关闭本轮测试标签页并停止测试服务；测试库与夹具保留以便复现。正式服务继续运行。

## 已实际验证

| 场景 | 页面观察结果 |
| --- | --- |
| 首次空库管理员 | 显示“创建首位管理员”，提交后进入零项目看板，人员管理仅有该管理员 |
| 管理员创建账户 | 逐一创建销售、设计、工艺、采购、生产、试验员、质量；对应人数与账户列表更新 |
| 八角色登录、退出 | 管理员与七类业务人员均可登录和退出；普通人员不显示人员权限入口；设计/销售有建项入口，工艺/采购/生产/试验/质量无建项入口 |
| 错误密码 | 显示统一的账户或密码错误提示，不进入工作区 |
| 客户及多规格建项 | 销售新增虚拟客户，编号自动大写；建项指定设计负责人和五类跨部门负责人，生成十阶段 Sheet；发起人保持销售、负责人保持设计 |
| 四项电机字段 | 两规格分别保存并显示出线、防护、绝缘、冷却；第二规格为右侧出线/IP56/H级/IC416，无电机编码要求 |
| 立项草稿与受控版本 | 不完整内容可保存草稿；再次保存需变更原因；缺少必填日期时错误提示且其他输入保留；补齐后提交，阶段版本和表单版本分别递增 |
| 历史版本比较 | 实际打开 V3 与 V2，显示状态/进度/操作人/时间差异，异步加载后显示一条受控表单记录差异 |
| 旧编辑窗口冲突 | 保留浏览器旧表单，再由独立 HTTP 测试客户端提交同一虚拟表单的新版本；浏览器旧草稿提交被拒绝，明确提示内容已更新，并保留“QA 旧窗口输入应保留”。这是浏览器 + API 辅助的双客户端测试，不冒充两个浏览器窗口成功保持上下文 |
| 阶段附件与失效会话 | 操作中经过超过八小时，会话失效；上传明确提示未执行及先保存输入，附件列表仍为零。重新登录并手动选同一夹具后上传成功，附件索引及阶段历史增加 |
| 试验员报告 | 页面提交带附件的型式试验报告，自动引用第一个电机的当前设计试验要求，显示提交人、当前 R1、文件索引及历史记录 |
| 质量检验 | 切换第二个电机时检验要求同步为 QA-I-02；提交带附件的整机检验记录后当前版证据齐套为 1/2，另一个规格仍提示缺项 |
| 阶段放行阻止 | 质量人员尝试把阶段设为已完成，被未完成的前置立项阶段阻止，调整原因仍保留在对话框；未误放行 |
| 看板筛选 | 实际选择设计负责人、半年度、月度、自定义模式，区间和指标同步；一个项目、两规格、立项90%时平均项目进度9%，责任人显示销售，与当前立项阶段一致 |
| 下载入口 | 实际收到原件、完整项目 Excel、程序 Word、单 Sheet Word、概览 Excel 的下载事件。ZIP 入口在新标签页处理，父页等待不能代表失败；在 ZIP 所属标签页确认下载事件。文件内容/CRC/原件字节校验由另行重跑的隔离生产运行测试负责，未将下载事件当作文件内容验收 |
| 时间修复复测 | 原页面将 UTC 02:14直接显示为02:14；修复后同一历史记录显示北京时间10:14，附件 UTC11:02显示19:02。仅改变显示，不改写历史数据库时间 |

日期控件自动化的直接 fill 在当前浏览器未触发受控状态提交；使用原生键盘上下调整确认后日期正确保留并提交。因此未将该工具输入差异直接当作产品日期控件缺陷。少量选择器/下载事件等待问题均按实际成功状态记录，未将尝试视为通过。

## 修复与发布验证

- 修改 `app/components/npd/ui.tsx`：统一将 SQLite 无时区 UTC、带 Z 和显式偏移时间显示为北京时间；日期字段本身不平移；新建表单默认日期及加天计算使用同一业务日。
- 新增默认测试中的时间用例，覆盖 SQLite、ISO/偏移、跨年午夜、空值/非法值、日期不平移、今日和加天。
- 完整 `npm test`、TypeScript、项目范围 ESLint、`git diff --check` 通过；封存候选的完整 `test:production-runtime` 通过。根目录 `npm run lint` 会扫入另一个选型工具的构建输出及无关临时脚本而失败，本轮未改动它们；项目范围检查明确排除了这些无关目录。
- 正式更新前备份：`backups/hengda-2026-09-07T11-32-14-046Z-gpxHW7`。启用 `20260907T110919Z-225bd30e6d4a` 后，启动日志核实版本，保活服务 running/PID94977，首页 HTTP200。
- 当前正式库与本次备份的17张npd表逐字段、逐行比较一致；quick_check=ok，外键错误0。保留原8账户、4项目、40阶段及历史，无业务数据迁移或依赖更改，无公网发布。

## 第二轮：反复变更、生产与质量、交接、订单和账号维护

沿用同一隔离库，先运行 `20260907T110919Z-225bd30e6d4a` 复现，修复后以 `20260907T121800Z-95190957b4bb` 重启并复测；未修改正式项目。

| 场景 | 实际页面结果 |
| --- | --- |
| 生产节点确认 | 设计新增系列通用端盖 QA-P-001 R1；生产账户确认完工后显示实际日期、确认人/时间、阶段V3及90%进度，不能修改已有零部件设计输出 |
| 零部件质量检验 | 质量账户选择零部件，检验要求自动引用；“让步接收”缺少处置说明时明确拒绝且保留表单；填写仅限试制的合成依据并上传夹具后成功，当前证据2/3 |
| 旧版问题复现 | 设计改端盖为R2，原生产确认清空、检验R1成为历史版，当前证据已是1/3；但阶段仍保留生产90%、质量60%，项目29%，确认进度存在失真 |
| 修复后零部件再次改版 | R2→R3保存后，生产节点0%、质量30%、项目17%在同次提交后同步；质量V6记录关联变更与1/3证据说明，节点责任人仍为质量，最后维护人为设计；历史R1报告和附件保留 |
| 规格改版 | 第一电机修改出线及试验要求R1→R2，验证进度45%→0%，质量保持30%，项目12%；旧型式报告显示R1历史版，当前R2仍待补齐，不误用旧结论 |
| 关联版本查看 | 实际打开质量V6详情，显示30%、修改人/时间/原因和11条领域数据；明确未有放行原件核验凭据，未冒充阶段放行 |
| 暂停/恢复 | 状态V1暂停为V2，显示只读提示，待办10→0，任务页单列暂停10项；恢复仍保留原周期和各阶段进度，后续项目操作恢复可用 |
| 负责人往返交接 | 设计交给销售后，负责人及看板筛选更新，发起人保持销售；设计保留成员身份但失去负责人专属入口，待办由10变6；销售登录后按交接V2交回设计，职责和原因正常保存，阶段进度/版本未因此改写 |
| 订单关联往返 | 销售录入虚拟订单QA-SO-20260907-01，客户/数量/日期保存；关联后待关联1→0，订单显示项目编号；解除后恢复待关联1，再次关联成功，可从订单打开同一项目 |
| 账号姓名/停用/启用 | 管理员将虚拟质量改名并停用，有效账户8→7，阶段提示新姓名“账号已停用；质量待分配”；已有检验内容保留。停用账号实际登录被拒绝。管理员恢复原姓名和启用，有效账户回8，原密码登录成功；未改邮箱或密码 |

修复将新增/改版后的目标投影用于当前设计证据统计：规格变更重算验证与质量，零部件变更重算生产节点及质量。领域写入、阶段进度/版本/快照、审计和项目汇总使用同一事务；保留受阻说明，已完成阶段转待复核。只按当前版本最新证据的齐套比例计至90%，阶段完成仍需前置门禁、表单复核和原件检查。没有在服务启动时批量重写现有业务进度，也没有自动替用户放行。

- 新增 `tests/design-progress.mjs` 及真实D1运行版，覆盖改版、范围增加的分子/分母、生产确认重置、旧证据保留、受阻保留、已完成转待复核、快照一致、过期阶段409、汇总和最后一步失败全回滚。
- `npm test`、真实D1 `test:design-progress-runtime`、TypeScript、项目范围ESLint和diff检查通过；同一封存候选完整 `test:production-runtime` 通过。新增专项已纳入默认事务测试链。未变更依赖或迁移。
- 本地更新前备份：`backups/hengda-2026-09-07T12-43-04-969Z-ZOtjrK`。正式启用 `20260907T121800Z-95190957b4bb`，运行日志确认版本，PID5136/running，首页200；17张npd表逐行逐字段与备份一致，quick_check=ok、外键错误0。
- 本轮结束时测试账户已退出、测试标签页已关闭、隔离服务已停止。虚拟账号恢复原姓名及启用状态，项目恢复进行且负责人交回设计；测试库/报告/订单/历史保留用于复现。

## 第三轮：失败评审与条件闭环放行

版本 `20260907T132644Z-94493b3cc64c`，同一独立53836库。由独立HTTP客户端以测试设计账号准备前三阶段五份有效合成表单并完成阶段，再提交SJ-05“不通过”作为浏览器负向夹具；该准备过程不算浏览器填写证据。

实际浏览器登录设计账号，进入评审页后显示“已提交·待处理”与“不通过，须整改并提交新的评审结论后再放行”。更新阶段选择已完成并填写调整原因，服务端拒绝，弹窗明确提示且输入保留，阶段版本仍V2。

随后在表单中改为有条件通过。一次自动化fill空字符串未清除旧跟踪结果，重开表单查明内容仍为先前合成记录，因此该次放行不能算“缺跟踪仍放行”的产品缺陷。改用填入单字符再原生Backspace，确认实际字段空白并提交，卡片显示10/11、待处理及所缺闭环内容；放行被拒，原因保留、阶段不增版。补齐合成跟踪验证结果后再次提交并由设计复核，评审V7/100%，当前节点转为零部件生产，总进度43%；失败和中间条件版本仍保留。

自动化完整十阶段及真实HTTP/R2/ZIP证据见完成度审计。它们证明对应接口和持久化流程，不替代尚未逐页完成的十阶段浏览器体验检查。技术条件的实际有效性由业务人员判断，不由“字段非空”自动认证。

本轮正式升级前备份为 `backups/hengda-2026-09-07T13-51-53-020Z-bi6nk0`，当前本地版本如上；PID11930/running、首页200，17张npd表逐字段不变。测试账号退出、标签页关闭、独立服务停止。

## 尚未通过的完整验收范围

仍需继续做全十阶段连续业务流程、改版后新报告补齐到重新放行、更多多人/多订单/多次返工组合、非成员实际页面及小屏/键盘完整可用性验收。账号密码/邮箱维护未在浏览器自动操作；对应鉴权与会话撤销有独立接口证据。上述第二轮已明确列出的生产、质量、账号停用、交接、关联往返和暂停恢复不再列为完全未验，但不能由单一合成样本扩大为所有组合均通过。

真实账户/项目由管理员核实；异盘备份保管安排、两项高等级依赖告警和长期运行边界仍见本地验收清单。本轮不构成“系统无bug”或完整正式验收完成的声明。
