# Word 完整开发档案视觉补验

核对日期：2026-09-09。结论：本次实际下载的 R2 模拟项目档案已完成 63 页逐页视觉检查及正文抽核，未发现正文裁切、重叠或中文缺字；仍有两项明确的打印/中文显示优化，不能记为全部版式验收通过。

本轮为只读验收，没有修改应用代码、依赖、数据库、原始受控模板或下载的归档文件，没有重新部署。正式本地固定版本仍为 `20260909T143856Z-eb16f1c8c49b`；结束时首页 HTTP 200，保活服务 running / PID 64529。

## 样本和范围

- 使用此前隔离浏览器 R2 重新归档流程实际下载的 `/Users/zhangyu/Downloads/NP-2026-002_QA-连续十阶段验收-0908_完整离线档案 (2).zip`。项目是明确标注为合成验收的 `QA 连续十阶段验收 0908`，不代表真实试验、客户签字或量产批准。
- ZIP 中唯一 Word 条目为 `01_开发程序.docx`。CRC 检查通过，逐字节提取至新的临时目录，后续确认提取文件仍与 ZIP 原条目完全相同。
- 两种规格：132S 当前 R2、160M 当前 R1，另有通用零部件 R1；保留旧版报告和重新放行历史。十阶段版本为 3 / 7 / 8 / 9 / 14 / 19 / 24 / 20 / 22 / 24。
- 此次没有新建或导出 DOCX。样本来自历史已验证的浏览器下载，不能表述为本轮又完成了当前正式库的浏览器导出。

| 对象 | SHA-256 |
| --- | --- |
| 原始 ZIP | `05712a7c8f13ec966772294c9d78f6afd8daa860ba046ce86ac6d87fc96805c3` |
| ZIP 内及逐字节提取的 DOCX | `5641620f29a1ce4edb6f6d0eb6f56b09c69596739d4b58ba60be28e569741528` |
| 正确字体环境下的验收 PDF | `f5d85ba49cba28a57bded7aa7f54b250af2212614017e658b3ee0e7a83c406af` |

## 渲染环境与先前异常

按 Documents 技能使用工作区依赖加载器返回的捆绑 Python、原生无界面 LibreOffice 和规范 `render_docx.py`，未调用用户桌面安装的 LibreOffice。

首次渲染产出 49 页，但字体配置未正确找到中文字体，出现中文缺字；这次结果不算通过。诊断确认本机已有 `/System/Library/Fonts/Supplemental/Songti.ttc`，无需安装字体或修改源文件。仅在本次临时目录创建 Fontconfig 配置，将宋体/SimSun 映射至 Songti SC，并为这一渲染进程设置 `FONTCONFIG_FILE`。字体匹配命令确认选中本机宋体后，同一 DOCX 重新渲染得到 63 页，中文恢复正常。未改系统字体、全局配置或应用导出字体。

内部复现材料位于 `/private/tmp/hengda-word-render-bLYCPE/`：

- `project-r2.docx`：原条目字节副本。
- `fonts.conf`：仅验收用字体查找配置；`font-cache` 为其独立缓存。
- `rendered-fonts/project-r2.pdf`、`page-1.png` 至 `page-63.png`：最终被检查的输出。
- `rendered/`：保留的首次错误字体结果，不用于通过结论。

这些是内部 QA 材料，不替换正式开发程序或作为新增用户交付附件。

## 检查结果

逐页查看了全部 63 张最终 PNG，而不是只看封面或文本抽取。页面尺寸均为 A4；全部 63 页页脚中的项目编号、当前页和总页数正确。另将 DOCX 的 1,448 段非空正文与 PDF 抽取正文作去空白匹配，全部找到；此项仅作为未遗漏正文的辅助证据，不代替视觉检查，也不证明业务事实或字段语义均已获批准。

| 页码 | 已检查内容 | 结果/边界 |
| --- | --- | --- |
| 1–5 | 封面、十阶段目录、成员职责、两种电机规格 | 型号、R1/R2、四项规格字段和长职责说明可读，表格无裁切 |
| 6–11 | 立项、设计任务及输入输出、版本摘要 | 标题和正文换行正常；各阶段独立起页会留下部分页末空白 |
| 12–18 | 开发计划、策划、设计评审及修改原因 | 长说明及多页历史可读；原受控表单中的历史字段未擅自删改 |
| 19–23 | 整机和零部件生产确认、历史进度 | 当前版次、姓名、实际日期及确认时间显示完整 |
| 24–30 | 验证表、三份新旧试验记录及附件索引 | 关联设计版次及文件名完整，长文件名在单元格内换行；附件类别仍显示内部代号，见待办 |
| 31–38 | 整机/通用件检验、四份新旧记录及附件索引 | 要求、设计引用、结果、检验员和时间可读；附件类别中文化待办同上 |
| 39–44 | 客户试用、签署占位和历史 | 正文未丢失；第 44 页只有“附件索引：暂无记录”及页脚，见待办 |
| 45–50 | 新品鉴定、设计/工艺/质量问题和变更历史 | 文本与页脚不重叠，长结论完整换行 |
| 51–57 | SJ-09 更改前后、影响范围和重新归档历史 | 新旧记录说明完整；不据模拟填写认可无更改归档新规则 |
| 58–63 | 订单空态、完整项目操作日志 | 日志时间/姓名/正文可读，最后一页正常结束；此样本没有关联订单，不证明订单明细视觉通过 |

该 DOCX 共 4 张表格，本样本中没有跨页表格。因此本轮不宣称已实际验证跨页重复表头、超长单行表格或所有单 Sheet 导出版式。未在 Microsoft Word/WPS 或其他平台实测；字体替代可能影响实际分页。

## 明确待办

1. **空附件提示孤页（打印质量）**：第 44 页仅一条“附件索引：暂无记录”。源导出将空态提示追加到整段历史之后，下一阶段又显式新起页。应调整空态位置或相邻段落的分页关系，不删除历史、不缩小字号、不修改原下载档案；须用有长历史且无附件的样本重新渲染确认。
2. **附件类别中文显示（可读性）**：第 30、38 页显示 `test_report`、`inspection_record`。后续应让 Word 附件索引复用现有中文类别定义，保留未知历史类别原文，不改数据库类别值；补充已知/未知类别导出断言及视觉复核。

以上均是可以继续在既有范围内处理的导出改进，不是新业务审批授权的替代。另行待用户确认的“无更改声明由负责人提交、管理员确认”仍未实施；真实资料复核、依赖告警及受控异盘备份安排仍按本地验收清单保留。
