# 新品开发系统 V2 完成度审计

## 2026-09-09 Word 长档案逐页视觉补验

- 对此前隔离浏览器实际下载的 R2 完整 ZIP 只读提取 Word，原条目逐字节不变。按 Documents 规范渲染并检查全部 63 页，1,448 段非空正文在渲染文本中均找到，A4 和全部页脚编号核对通过；不据此认定业务内容获正式批准。
- 首次中文缺字来自验收渲染器的字体查找，局部使用本机既有宋体后恢复；未安装字体、修改全局配置或源 DOCX。错误字体的 49 页结果未计为通过。
- 定位两项未关闭优化：第 44 页空附件提示孤页，第 30/38 页附件类别仍为内部英文代号。本轮没有跨页表格、关联订单明细或其他 Office 客户端的视觉证据，不能宣称全部 Word 版式通过。
- 无应用源码、数据、依赖、模板或部署修改；当前版本 20260909T143856Z-eb16f1c8c49b 保留，结束时首页200、PID64529/running。无更改归档等业务待确认项不变，完整目标仍在进行。详见[Word视觉补验](word-visual-acceptance-2026-09-09.md)。

## 2026-09-09 试验与质量两步提交恢复

- 试验及质量弹窗保留既有附件复用，新增已保留文件/原目标/版次提示，不把附件成功等同记录成功。同步引用锁修复同一回调在下一渲染前重复调用会两次上传的问题，覆盖后续记录请求；失败解锁，表单继续保留。
- 换文件/规格/整机零部件、清空文件、上传失败、记录未知结果和新props不能改变旧基准版次均有实际回调断言；16项新专项及阶段上传/弹窗共26项通过。新提示转义、读屏status和长文件名换行有组件/样式证据，无新增浏览器实测。
- 最终完整npm test、TypeScript/范围ESLint/diff和封存候选完整HTTP通过。三种记录上传后故意失败，原件下载逐字节不变；补正复用同一ID，仅3附件/1试验/2检验，并继续十阶段100%及ZIP归档。未将内存复用扩大为跨会话恢复或全系统恰好一次提交。
- 备份hengda-2026-09-09T14-40-11-400Z-0PrjB6后启用20260909T143856Z-eb16f1c8c49b；17表结构和全部原值一致，quick_check正常、外键0，60文件校验通过，日志匹配、PID64529/running、首页200。无迁移、依赖或正式资料修改。
- Sites错误恢复规范影响了部分成功回执设计。再次询问无更改归档审批方案但未收到确认，原SJ-09规则不变；其他待办保留。详见[两步提交补验](evidence-dialog-acceptance-2026-09-09.md)。

## 2026-09-09 登录失效与保存结果提示

- 已获成功回执但刷新401/异账号/网络失败时明确“已保存”及下一步；写入前认证/账号上下文拒绝仍明确“未执行”。断网、异常成功回执和5xx不推定失败，标注结果待核对且不自动重试，未提交表单不关闭。
- 重要提示不自动消失，提供关闭按钮及读屏通知；后续提示可替换。新通知取消旧计时器并在卸载清理，防止前一通知抹掉后一通知。没有浏览器草稿自动存储或页面强制跳转。
- 53项身份/认证/实际组件专项、TypeScript/范围ESLint/diff、完整npm test与构建通过。封存候选完整HTTP通过，新增合成管理员自身修改200→旧会话401→新密码登录核对姓名与版本，正式账户未改动。本轮没有浏览器/Office视觉新增证据。
- 备份hengda-2026-09-09T14-29-40-034Z-bW1OmY后启用20260909T142813Z-e8caf04ca752；17表结构及全部原值一致，quick_check正常、外键0，60文件校验通过，日志匹配、PID61257/running、首页200。无迁移、依赖或远程发布。
- Sites状态规范影响了结果区分及提示行为。其他业务审批、旧资料、依赖和保管待办继续保留，整体目标未完成。详见[保存结果补验](write-feedback-acceptance-2026-09-09.md)。

## 2026-09-09 订单逾期及人员更新时间口径

- 固定时钟React渲染先复现两项失败：北京时间凌晨订单交期用UTC日期少算一天；人员updatedAt直接截取UTC前缀显示前一天。复用既有businessDate/formatDate修正，不改原始时间戳。
- 订单先验证日期再判逾期，空白和非法旧值提示“日期待核对”并保留原文，当天/未来/已完成不误标；人员日期提示提供北京时间分钟值。已有表单默认日期跨月/年/闰日验证通过，不重写原有函数。
- 15项控件专项在默认环境及UTC/洛杉矶时区通过；最终完整npm test、TypeScript/范围ESLint/diff、构建及候选完整production-runtime通过。本轮无浏览器午夜实测或Office视觉证据。
- 备份hengda-2026-09-09T14-14-59-071Z-r3ECp4后启用20260909T141326Z-ed1935891cdc；17表结构及全部原值一致，quick_check正常、外键0，60文件校验通过；日志匹配、PID58766/running、首页200。无迁移/依赖/真实资料变更。
- 保留无更改归档、旧资料、依赖及运行保管待办，不据局部日期修复判定总体完成。详见[日期口径补验](business-date-acceptance-2026-09-09.md)。

## 2026-09-09 账户及项目操作日志权限边界

- 独立夹具复现普通人员收到他人全局管理日志、全局300条截断使可见项目活动被挤掉。SQL改为同事务当前有效账户授权，先限制发起/负责/参与项目及本人全局记录，再稳定倒序取300条；管理员全局范围保留，原始日志与完整项目归档不截断、不改写。
- 内存/真实D1新专项、TypeScript/范围ESLint/diff、完整npm test和封存候选完整production-runtime通过。全量回归发现上一轮导航Hooks变动导致身份测试桩缺少导出；补齐测试桩并保留原断言，6项跨账号回调测试及全套回归重跑成功。
- 同一候选实际HTTP规模夹具通过：100项目、1000规格、2000部件、1000阶段/历史、1000项目日志；单项目人员完整获得本项目10条活动，其他99项目不混入或挤占。读取33ms为本机单次样本，不作长期容量/浏览器性能保证。
- 备份hengda-2026-09-09T14-06-36-882Z-PwSqmr后本地启用20260909T140357Z-ecbb73137166。17表结构/所有原值一致，quick_check正常、外键0；60文件校验通过，日志匹配、PID56137/running、首页200。无迁移/依赖/真实人员资料变动，无远程发布。
- 本轮只验证D1和真实HTTP，没有新增浏览器或Office视觉证据。审批规则、旧表单、依赖及备份保管待办继续保留。详见[日志范围验收](activity-scope-acceptance-2026-09-09.md)。

## 2026-09-09 可读性与窄屏节点信息补强

- 修复看板紧凑布局隐藏负责人/阶段/进度、项目关键指标隐藏及状态标签继承图标定宽的问题；常用字号至少14px等效rem，正文/输入16px，改善文字对比。任务、阶段元信息、工具栏和长弹窗自适应，未改变业务口径或数据。
- 实际视口320/390/768/1280px看板无页面横溢，最终候选补验390px任务姓名/阶段/日期、320px项目表单滚动与字段、1280px看板；移动菜单经过真实失败复现后修正，打开聚焦关闭按钮、Escape关闭并返回入口已验证。未将视口宽度测试当作200%文字放大或全页面验收。
- 第一候选完整npm test通过；最终12项控件专项、TypeScript/范围ESLint/diff、构建和候选完整production-runtime通过。浏览器warn/error为空，未提交模拟业务数据；退出测试账户、恢复视口、关闭测试页，独立PID52472停止。
- 备份hengda-2026-09-09T13-54-34-549Z-EbuUr3后本地启用20260909T134539Z-45a1dcd2016b。17张npd表结构和所有字段与备份逐值一致，quick_check正常、外键0；60文件校验通过，日志版本匹配、PID53579/running、首页200。无迁移、依赖或真实资料改写。
- Sites可读性规范影响了字号、信息保留和紧凑布局；仅本地更新，未远程发布。无更改归档审批规则、旧表单业务复核、依赖和备份保管边界仍保留。详见browser-acceptance-2026-09-09.md第四轮。

## 2026-09-09 只读入口对齐与R2重新归档闭环

- 暂停/关闭项目概览入口与既有服务端权限对齐，新增只读及恢复指引；保留授权人员的暂停恢复、交接和管理员已关闭项目处理能力，不扩权。
- 9项控件专项、TypeScript/ESLint/diff、完整npm test和候选完整production-runtime通过。独立浏览器完成已归档双规格项目中仅132S R2变更，再按设计/生产/试验/质量/销售逐阶段重新放行至100%。旧45条历史、5份附件元数据、原报告/检验、未变更160M及共用件逐字段不变。
- 实际浏览器下载310708字节完整ZIP，CRC及7份新旧原件逐字节核验通过；十阶段版本3/7/8/9/14/19/24/20/22/24。该路径关闭“单规格改版后补齐至重新归档”的待验项，不代表全部复杂组合或办公软件视觉已验证。
- 备份hengda-2026-09-08T19-27-53-547Z-LZcPoW后本地启用20260908T190237Z-a8d9b71388f2，启动日志匹配、首页200；17表结构和所有字段与备份完全一致，quick_check正常、外键0。无依赖、迁移、真实人员或业务资料修改。模拟账户退出、标签关闭、独立PID54526停止，合成库保留。
- 无更改声明审批规则仍未实施或追认。原有23份旧表单、剩余依赖风险、真实资料确认和受控异盘备份安排继续保留。详见browser-acceptance-2026-09-09.md第三轮；整体目标不据局部验证判定完成。

## 2026-09-09 阶段状态提示及ZIP直接下载修复

- 试验/质量证据提示与各自项目阶段状态对齐；齐套不等于放行，已完成不再固定写待复核，受阻/缺阶段/零目标和旧完成缺证据分别提示。原备注以“记录时内容”标签保留，不改写审批历史或业务门禁。
- ZIP入口去掉新窗口target，保持原生流式下载和原有鉴权。候选浏览器普通点击下载成功；实际保存文件111667字节CRC通过、十阶段版本完整、5份附件逐字节等于合成原件。前轮downloadMedia实际文件亦确认完整，不把此前普通点击超时混算为通过。
- 13项证据/控件专项、TypeScript/ESLint/diff、完整npm test及封存候选完整production-runtime通过；已完成与改版待补两类项目实际页面核验，浏览器无warn/error。无模拟业务保存。
- 备份hengda-2026-09-08T18-52-33-943Z-qwvT9y后本地启用20260908T184720Z-26c0d604ba41。17表结构/全部字段与备份一致、quick_check正常、外键0，60文件封存校验通过；无迁移、依赖或实际账号/资料修改。详见browser-acceptance-2026-09-09.md第二轮。
- 更新后PID52048/running、日志版本匹配、首页200。临时标签关闭、模拟账户退出、独立服务PID51684停止，测试库保留。本轮普通ZIP下载完整交付及显示问题已关闭，不延续前轮相同待验项。
- 无更改归档规则仍待用户确认，未实施。历史业务资料、其他未覆盖流程、依赖和运行保管边界未据本轮消除，完整目标仍未达成。

## 2026-09-09 解锁后补充下载及按钮交互验收

- Mac已解锁，固定版20260908T154043Z-871b1614e904在独立53836/既有合成库继续验收；2项目49%/100%、看板75%及十阶段完成状态保留。
- 浏览器实际点击项目/概览Excel、完整/单Sheet Word、试验与零部件检验附件均触发下载；成员、指定电机输出、指定零部件变更及生产确认弹窗实际打开且取消，设计/生产确认入口区分，登录退出通过。本轮无业务保存。
- ZIP普通点击的当前标签下载事件超时，target=_blank尚不能据此判断普通点击交付；公开文件链接下载接口成功。独立模拟HTTP复核111670字节ZIP、CRC、十阶段版本和5份原件逐字节一致，保留两类证据边界。
- 记录已完成阶段仍显示“待阶段复核”等提示的显示不一致，未改写阶段结果或历史。归档无更改审批规则仍待确认，Mac锁屏条件已解除。详见browser-acceptance-2026-09-09.md。本次无源码/依赖/迁移或部署更新，不判定整体目标完成。

## 2026-09-08 无更改归档路径诊断及规则待确认

- 上轮属实际进展：连续十阶段浏览器验收、本地更新及17表原值核验均完成。本轮核对原控制程序第5.9和第6条，发现系统将SJ-09无条件设为所有项目归档必交，缺少无更改项目的受控归档路径。
- 全新隔离内存夹具复现：R1规格、前九阶段设定完成，真实归档方法拒绝缺SJ-09；空白更改正文又触发8项必填。失败前后五组项目数据/历史/活动逐值相同。此为归档规则诊断，不宣称前九阶段均由该夹具正常审批完成。
- 建议负责人填写核对范围及依据的无更改声明，管理员确认后归档；有已知更改证据不得覆盖，后续更改需撤回归档并留痕。该新增企业审批/表单适用性规则待用户确认，尚未更改业务门禁。
- Mac再次确认为锁屏，不能继续浏览器下载或新按钮交互。当前版本20260908T154043Z-871b1614e904保留，本轮无业务、账户、原模板或服务变更。详见archive-applicability-decision.md。

## 2026-09-08 连续十阶段浏览器验收与操作名称补强

- 新建独立模拟项目NP-2026-002，浏览器逐字段提交十受控表单，设计/生产/试验/质量/销售依次协作，完成两整机、一通用件、两份试验、三份检验及五份合成附件，十阶段均100%，项目completed。只读持久化复核含45条阶段历史。没有通过接口预填该项目。
- 缺少SJ-06时虽两规格报告/附件齐套，完成阶段仍被拒绝；补表后才放行。所有签名占位、检验和批产意见均明确为QA合成，不代表实际批准。企业研发本路径未关联订单。
- 修复无名团队/设计输出/检验附件控件，其他零部件/报告操作名称带对象上下文；生产确认状态及附件类别中文化，未知历史值保留。4项新增渲染专项、完整npm test、TypeScript/ESLint/diff和封存候选完整production-runtime通过。
- 备份hengda-2026-09-08T15-42-55-641Z-Z7QGBO后启用20260908T154043Z-871b1614e904；PID36312/running、日志版本匹配、首页200、60文件封存校验通过。17张npd表结构/全部字段与备份一致，quick_check正常、外键0。未改依赖/迁移/真实资料或远程发布。
- 浏览器下载前Mac锁屏，需用户人工解锁；未绕过，也未将候选HTTP导出通过当作此项目浏览器下载通过。独立测试页保留，测试服务停止，合成库保留。升级后按钮实际交互和本轮下载仍待补验。
- 总目标仍未标记完成：正常无变更归档路径、改版后重新放行的更多组合、真实旧资料核实、剩余依赖告警及异盘备份保管安排不能据本轮消除。详细记录见browser-acceptance-2026-09-08.md第四轮。


## 2026-09-08 录入防误关闭及键盘可靠性

- 18个编辑弹窗统一接入保存中锁定和编辑后离开确认；关闭、取消、Escape及遮罩点击走同一守卫，结构增减也标记编辑。继续编辑不重建输入子树，成功按原流程关闭，失败保留内容；只读窗口直接关闭。没有客户端草稿/密码持久化、自动重传或对已保存记录的删除。
- 原生dialog隔离背景，首尾Tab循环、初始标题焦点、退出返回入口、保存失败后恢复操作控件；本轮浏览器分别发现并修正原生末尾Tab进入浏览器栏及inert解除后焦点丢失，未忽略观察异常。
- 首个候选完整npm test通过；最终改动后7项Modal专项加17项渲染/任务专项共24项通过，TypeScript、范围ESLint、diff、构建通过；最终候选完整production-runtime一次通过。独立浏览器实测建项输入/结构增减的离开保护、继续保留、Escape/Tab、SJ-06失败保留→草稿V1、重新读取、失败焦点恢复、只读关闭及退出。390宽确认框无横向溢出；未声称全小屏或强制断网/断电恢复验收。详见[第三轮记录](browser-acceptance-2026-09-08.md)。
- 备份 `backups/hengda-2026-09-08T15-14-44-849Z-ilXtff` 验证后启用 `20260908T150953Z-710f9b0f1c66`；PID32482/running、日志匹配、首页200。17张npd表结构及所有字段与本次备份一致，quick_check正常、外键0。无迁移/依赖升级/正式资料补填或公网发布。
- 本轮解决录入安全与键盘体验缺口，不替代全十阶段逐字段业务验收、旧资料复核、依赖告警处置或异盘备份保管安排。整体目标仍未据本轮局部验收判为完成。

## 2026-09-08 历史版本字段可读性并完成本地更新

- 历史记录已知字段/状态中文化，字段时间按北京时间保留到秒；表单正文使用对应受控表单标签。未知字段/值和无效历史时间保留，不改变差异判定；原值入口保留UTC与枚举原文，历史人员标识不反推当前姓名。快照、存储、权限和导出逻辑未改动。
- 新增与既有revision测试共6项、TypeScript、范围ESLint、diff及构建通过；固定候选完整production-runtime一次通过。隔离浏览器核验V10/V9返工差异、10:24:33→10:26:04北京时间及02:24:33原值、SJ-01中文字段，截图布局可读，采集warn/error为空。详见[第二轮验收](browser-acceptance-2026-09-08.md)。
- 备份 `backups/hengda-2026-09-08T14-54-40-339Z-UK4cli` 验证通过后启用 `20260908T144833Z-05dcd3253432`；PID29291/running，日志匹配，首页200。17张业务表结构与全部值等于本次备份、quick_check正常、外键0。隔离账户退出、临时标签关闭和测试服务停止。
- 本轮无迁移、依赖/原模板修改、正式项目补填或远程发布。完整目标的剩余业务资料复核、未覆盖界面场景、安全告警和运行保管边界仍保留，不据本轮显示优化宣称全部正式验收完成。

## 2026-09-08 整机节点版本验收并完成本地升级

- 用户再次允许后，本机运行权限恢复；整机生产及设计改版真实D1专项通过。候选完整HTTP首次在跨站拒绝附件请求处ECONNRESET，已退出；全新隔离库复测一次全部通过，没有放宽断言。具体范围和瞬时失败记录见[本轮验收](browser-acceptance-2026-09-08.md)。
- 浏览器生产账号逐条确认两规格及一部件，阶段0→30→60→90%，复核完成100%；整机返工后退回60%，实际日期清空，V10与V9比较保留原完工及说明。Excel和Sheet Word下载事件通过；设计负责人无生产确认入口，登录/退出通过。测试账号退出、标签关闭、独立服务停止。未声称全十阶段逐字段浏览器或所有组合验收完成。
- 升级前成套备份 `backups/hengda-2026-09-08T02-29-39-371Z-m1IWgQ`；启用 `20260907T143736Z-cf3975a22f9e`。PID69359/running、日志版本匹配、首页200、60文件校验通过。
- 0010已执行；对比备份，17张npd表所有旧字段原样保留，新增整机确认字段NULL/空串，不虚构确认；quick_check正常、外键0。8账户/4项目/7规格和40历史版本不变，无依赖/原模板修改或远程发布。
- 下列2026-09-07“待验、未上线、授权受阻”记录为当时历史，已被本次运行/部署结果替代。完整目标的业务资料核实、其他未覆盖验收及运行保障边界仍按本地验收清单保留，不据本轮升级宣称系统零bug或正式业务全面验收完成。

## 2026-09-07 整机归档时区与页面结构补验（仍未上线）

- 上轮属于实际进展：整机生产确认实现及迁移/事务测试完成。端口运行审批原请求和一次重试均超时，当前没有新增人工授权；本轮没有再次提交同一权限请求，也没有绕过限制启动测试服务。
- 离线导出专项新增真实Store确认路径后，复现Excel新增“生产确认时间”未被时间表头集合识别，输出UTC文本而不是北京时间日期单元格；测试先以`t="s"`失败。已纳入既有北京时间转换，验证生成XLSX的V2单元格为日期数值，换算与`2026-09-07 08:01:02`一致，保留秒，不把文字公式化。
- 将整机生产状态文案移到共享领域模块，页面、Excel、完整Word及Sheet 5使用同一逻辑；旧completed缺确认凭据明确为“历史完工·待生产复核”，不输出无条件生产完成，不改写原业务字段。
- Excel专项校验确认人、时间列、说明换行/转义及历史待复核标记；Word专项新增独立production.docx，验证全系列规格、确认信息、北京时间、中文特殊字符，并确认单Sheet不混入立项/验证表单。均为内存合成档案及文件结构/内容检查，不宣称办公软件视觉排版或浏览器下载已验收。
- 新增motor-production-render专项：多规格操作按钮具独立可访问名称，查看模式无按钮、空态不提供伪操作；确认对话框保留基准R/V、风险说明、必填原因，日期仅完工状态必填且以当天为上限，页面确认时间为北京时间。静态React渲染通过，不替代真实点击、键盘或屏幕尺寸验收。
- 本轮store、archive-snapshot、workspace-history、stage-originals、collaboration-versions、transactions、lifecycle、ownership、design-progress、form-release、motor-production、clean、export和login-security离线链通过；TypeScript、范围ESLint、diff及生产构建通过。没有将未重跑的真实D1/HTTP或依赖安全扫描写成通过。
- 新候选 `20260907T143736Z-cf3975a22f9e` 替代上轮待验候选；仍未激活。0010迁移内容未再变，依赖锁定未变，现用服务指针和业务库不改动。后续仍须获准运行本机端口，完成真实运行和浏览器验收，再备份升级；完整目标不能据离线测试判定完成。

## 2026-09-07 整机生产确认候选（未切换日常版本）

- 补上原第9项缺口：Sheet 5逐规格整机生产确认，生产/管理员专属；记录状态、实际完工日期、实名、服务端时间、说明及完整版本快照。项目概览使用中文生产状态；旧完工记录缺确认凭据显示待复核，不自动补签。
- 整机与零部件共同计算生产阶段0～90%齐套进度；放行需两类节点均非空且逐条有完工日期及确认凭据。改版重置该整机确认并重算生产/试验/质量进度；返工撤回下游完成状态，历史确认/报告保留。确认、阶段、项目汇总和审计同事务，旧版提交及并发写入受现有版本守卫保护。
- Excel电机规格表追加确认人、时间、说明；完整Word及Sheet 5归档新增整机生产节点明细。修正通用日期校验，拒绝不存在的日历日期。
- Drizzle生成0010_plain_lake增量迁移，增加3个字段；只在空库补建原有表结构后执行新字段增量，沿用既有兼容方式；0005～0009未改写。迁移专项证明旧行逐字段保留、无伪造签认、外键约束、失败完整回退、重启不覆盖新确认。
- 完整npm test通过；后续新增迁移专项及整机专项再次通过。TypeScript、范围ESLint、diff检查通过；封存候选 `20260907T142341Z-d623a69cca55`，未激活。
- 整机专项覆盖六类非生产角色拒绝、无效/未来/空日期、必填说明、版本冲突零写入、实名归档映射、整机未确认阶段拦截、全部确认放行、返工下游撤回、改版清空确认、末步故障回滚、并发仅一成功、暂停和非成员隔离。十阶段内存集成已加入整机确认并达到100%；HTTP专项也已扩展，但本轮尚未运行。
- 真实D1测试监听127.0.0.1被沙箱EPERM拒绝；请求本机隔离运行权限及一次重试均为自动审批超时，不视为业务测试失败或安全拒绝。故未执行本轮真实D1/候选HTTP/浏览器验收，未停止/升级现用服务，没有对业务库执行0010迁移或写入。现用指针仍为 `20260907T132644Z-94493b3cc64c`；launchctl只读状态running、PID11930，日志与原版本一致；受限环境curl失败，不能据此声称本轮HTTP健康已验证。
- 下一步：取得本机测试端口执行许可，运行整机/改版真实D1及候选production-runtime，隔离浏览器确认UI/导出；通过后停服成套备份、激活候选，并逐字段验证旧业务数据保留。继续保持本地部署，不对外发布。

## 2026-09-07 受控表单有效性与失败结论放行修复

- 独立库先复现旧逻辑错误：SJ-05评审结论“不通过”成功提交后，updateProjectSheet仍允许阶段completed；新增回归在旧实现下报Missing expected rejection。原门禁仅检查表单status，没有校验字段或技术结论。
- `lib/forms.ts` 新增提交校验：必填非空白、文字/布尔/数值类型、非负有限数、正整数试样数、真实日历日期、枚举/多选子集且不重复、立项及计划起止顺序。草稿可不完整，容器仍必须是字段对象；未知历史扩展字段保留。正式提交允许“不通过/不同意”，不强迫将失败结论改成通过才能留痕。
- 放行重新核验已提交正文，覆盖旧版本曾接受的非法内容。SJ-05评审不通过、SJ-06验证不通过、SJ-08不同意批产、SJ-10技术确认不通过均禁止放行。有条件评审需跟踪结果/验证人/日期，有条件验证需跟踪结果，整改批产需纠正措施跟踪验证，整改技术确认需评审汇总及采纳依据；这些是结构性闭环门槛，技术充分性仍由有权限的责任人复核，并非系统自动认定整改有效。
- 表单卡片共享同一规则，显示“已提交·待处理”和具体原因，不再用正常已提交样式掩盖失败/缺项；字段统计不计空白。领域数据、阶段/项目汇总、版本与审计仍走现有事务和并发守卫；没有自动补全、撤销或回写历史业务状态。
- `tests/form-release.mjs` 从新建两规格项目开始，连续提交十表单、生产确认、试验及整机/部件检验，十阶段放行至100%；四类失败/条件缺项拒绝且全状态不变，整改后成功；已完成项目失败复评会重开并撤回七个下游阶段，重新逐阶段放行成功。专项内原件为明确的元数据替身。真实D1运行专项通过。
- `tests/form-validation.test.mjs` 覆盖类型、空白、枚举、多选重复、日期/闰年、数量和先后顺序。旧store/originals/lifecycle测试中人为置submitted的空JSON夹具补成有效合成表单，保留原报告失败/缺件/回滚断言；不放宽生产校验来迁就旧夹具。完整npm test、TypeScript、范围ESLint、diff通过。
- 新增 `tests/form-release-http.mjs` 并接入封存候选的完整production-runtime：全新库实际HTTP建项、十表单、无效日期400/零写入、失败评审400/零写入、生产确认、真实附件存储三份合成原件及核验凭据、十阶段100%、最终ZIP的CRC/清单通过。旧鉴权、八角色、会话、订单/交接、附件及导出测试也通过。
- 浏览器前置1～3阶段由独立HTTP客户端准备，不能计作浏览器逐字段填写。实际页面验证评审失败提示、完成拦截/原因保留、条件跟踪缺项拦截、补齐后阶段完成/当前节点转生产。一次自动fill空字符串未清空React记录，读取后确认旧跟踪字段仍在；以原生Backspace清空并核对字段10/11后重测通过，没有把工具空填差异算成产品门禁失败。详见浏览器验收记录第三轮。
- 备份 `backups/hengda-2026-09-07T13-51-53-020Z-bi6nk0` 后启用 `20260907T132644Z-94493b3cc64c`。日志确认版本，PID11930/running，首页200；17张npd表逐字段与备份一致，quick_check=ok/外键0。无迁移、无依赖升级、无远程发布。测试账号退出、标签页关闭、独立服务停止。
- 正式旧库只读核验：23份submitted表单均存在新校验项未满足，所在阶段均标completed；没有据此声称业务已验收，也未自动更改它们。管理员需核对历史资料与结论。
- 后续明确缺口：当前源码只有零部件confirmPartItem生产确认接口，电机规格虽有status/actual_date字段，但未找到独立整机生产完成确认的业务写入入口。用户原要求包含整机及零部件时间节点确认，必须继续补齐并验证，不能仅凭零部件确认测试标为整体完成。

## 2026-09-07 设计改版后的进度联动与第二轮浏览器验收

- 实际浏览器复现：已完工端盖R1带让步接收检验，改R2后旧记录已成为历史版、当前证据1/3，但生产阶段仍90%、质量60%，项目汇总29%。修复 `db/store-v2.ts`：新增/改版前按提交后目标投影重算零部件确认比例及当前版证据比例；规格影响验证和质量，零部件影响生产节点和质量。
- `db/revision-transaction.ts` 在原乐观并发/权限事务中同步形成关联阶段版本与快照，记录修改前状态/进度/说明和提交后领域数据，保留受阻说明、撤回已有完成结论，项目总进度同批更新。失败全部回滚；没有启动时静默修复正式记录或擅自阶段放行。
- 修复后实际R3保存显示生产0%、质量30%、项目17%；电机R2后试验45%→0%、质量30%、项目12%，旧试验/检验及原件仍保留。页面通过生产确认、让步接收缺依据拦截/带附件提交、暂停/恢复、负责人往返交接、订单关联/解除/重关联和账号改名/停用拒绝登录/恢复登录。完整证据与边界见[浏览器验收记录](browser-acceptance-2026-09-07.md)。
- 新增 design-progress 及真实D1专项，默认test链涵盖分子/分母、扩项、阻塞保留、撤回放行、快照、旧窗口冲突、汇总、末步故障回滚。完整npm test、真实D1专项、TypeScript、范围ESLint、diff及最终候选完整生产HTTP测试均通过。
- 备份 `backups/hengda-2026-09-07T12-43-04-969Z-ZOtjrK` 后启用 `20260907T121800Z-95190957b4bb`；日志核实版本、PID5136/running、首页200。17张npd表所有字段与备份一致，quick_check正常、外键错误0。无迁移、无依赖变化、无公网发布。测试账户退出、标签页关闭、隔离服务停止，合成夹具保留。
- 总目标仍保留：完整十阶段连续验收及重新放行组合、实际资料确认、备份保管安排与残留依赖风险未关闭；不声称零bug或正式全面验收完成。

## 2026-09-07 用户授权后的浏览器验收与北京时间修复

- 在独立 D1/R2 和端口53836完成首次管理员、七类业务开户/登录退出、多规格及跨部门建项、立项草稿/提交/历史比较、附件及导出、试验和整机检验、看板周期及负责人筛选。真实浏览器旧表单配合独立HTTP客户端验证冲突拦截且输入保留。具体通过项、工具限制和未覆盖项见[本轮浏览器验收](browser-acceptance-2026-09-07.md)。
- 浏览器确认页面将SQLite UTC时间直接截断显示的问题；统一界面北京时间及新建默认业务日期，保持数据库时间不变，补充跨日/偏移/非法值测试。完整npm test、TypeScript、项目范围ESLint、diff及封存版本生产HTTP回归通过。
- 备份 `backups/hengda-2026-09-07T11-32-14-046Z-gpxHW7` 后启用 `20260907T110919Z-225bd30e6d4a`，日志确认实际版本，首页200，PID94977/running。17张npd表全部字段与备份一致，quick_check正常、外键错误0。无依赖或数据库迁移、无真实页面操作、无远程发布。
- 测试账户退出、测试页关闭及独立服务停止，合成测试库保留。浏览器授权阻碍已解除；尚未覆盖的连续业务场景、真实资料核实、备份保管和剩余依赖风险未关闭，不标记总体目标完成。

## 2026-09-07 迁移工具依赖定向修复

- 现有依赖图确认Drizzle Kit 0.31.10本身使用esbuild 0.25.12，只有@esbuild-kit/core-utils 3.3.2分支仍为0.18.20。对照上游公告修复版本0.25.0，在 `/private/tmp/hengda-migration-deps-qyjHoL` 独立副本用npm override将该helper的esbuild固定为0.25.12；保留其他框架/工具版本，不采用审计建议的Drizzle降级。离线安装禁用安装脚本，改变2个实际包，27个锁条目均限于该分支及平台二进制。副本无真实数据或环境密钥。
- 新增 `tests/migration-tooling.mjs` 并纳入默认npm test：检查实际解析版本、同步/异步TS转译和映射；全新临时目录的实际Drizzle CLI验证29表结构无变化、历史SQL及快照字节相同；合成新增字段仅生成一条常量默认值ALTER，journal仅追加，内存SQLite执行后全部旧值保留，完整性/外键通过，重复生成无变化。旧/新编译器均通过同一用例。早期夹具绝对out路径遇到工具既有错误且exit0，调整为本项目采用的相对路径，并要求成功文本和实际内容，不用退出码冒充通过。
- 副本完整npm test、TypeScript、完整隔离HTTP、真实D1空库初始化/重启及事务回归通过；最终候选 `20260907T004244Z-efeaf8e9a302` 封存后HTTP再次通过。无浏览器/DOM验收。app/db/scripts/drizzle与工作区逐目录相同，没有新增真实业务迁移或变更既有业务规则。
- 预先复制候选完整node_modules到专用准备目录并diff逐目录核对一致。暂停已核实的本地托管服务，备份 `backups/hengda-2026-09-07T00-46-40-895Z-Lt9Lik`，使用apply_patch同步package及锁文件；旧node_modules及旧清单保留到 `.local-releases/.runtime-backup-JO2HLw`，再启用候选依赖/固定版本。退出保护可在未启用时恢复本次补丁及依赖，实际成功切换。没有删除旧环境、运行中安装、数据库迁移或公网发布。
- 新运行锁摘要 `675984ca9b84a10bae2deaa2bf98eb3aedb00a26cd831ec6e77fc9ac31ce2275`，Node25.6.1和Miniflare4.20260722.0不变。日志确认新版本，PID44542/running、首页200。17张npd表所有字段与备份相同，quick_check正常、外键错误0，8账户/4项目/40阶段及40历史保留。根目录完整npm test、TypeScript、定向ESLint和diff检查也通过。
- 公开审计候选UTC00:35:56与本地启用后UTC00:49:24均为0中等、2高、0严重，仍requiresAttention。旧esbuild相关4项中等受影响包告警消除；image-size和vinext高等级告警保留。旧恢复目录仍含旧依赖，未在当前运行图中使用，不宣称机器内无旧包或系统已零风险。安全评估、README和当前验收清单已同步；浏览器授权、真实业务验收及剩余风险仍未关闭，总体成熟度目标继续保持未完成。

## 2026-09-07 图片解析构建期缓解

- 保留现有Sites/Vinext架构和本地模式。在不含实际业务数据、附件、备份或密钥的 `/private/tmp/hengda-parser-mitigation-QfmZrT` 副本评估公开disableTypes接口。Vite配置函数在创建框架插件前禁用icns/heif/jxl/jxl-stream，HEIF包含HEIC/AVIF。将现有image-size 2.0.2声明为直接开发依赖；锁文件仅新增顶层一行声明，无实际包版本变化，无下载或node_modules改写。
- 新增 `tests/image-policy-runtime.mjs`，子进程20秒/192MiB边界，加载真实Vite配置并调用实际Vinext图片导入插件和元数据路径。正常PNG/SVG与6类受限样本均通过；错误后缀.png不能绕过识别。新增显式 `tests/image-policy-build.mjs`，仅在规定临时副本生成不存在的icon.png，真实构建因disabled file type主动失败而非超时；临时夹具随后移除，失败构建未发布。两个测试不向HTTP或真实R2写入异常样本，错误测试不在业务源码目录制造图标。
- 重新正常构建后的副本完整npm test、类型检查通过；当前构建与封存候选 `20260907T001644Z-8155d24cc39e` 各自完整隔离HTTP回归通过，含账户、权限、阶段/协作版本、负责人交接、上传下载及Word/Excel/ZIP。业务app/db/lib/scripts/worker与原工作区逐目录比较相同；本项不改变报告附件接受规则。根目录同步后也重新完整npm test、TypeScript、定向ESLint通过。
- 暂停已核实的亨达托管服务后，备份 `backups/hengda-2026-09-07T00-19-38-176Z-2022uO`；使用apply_patch同步已验证的配置、依赖声明和测试命令，再验证/启用候选，恢复服务。期间准备了未启用时恢复本次三文件补丁的退出保护，实际正常成功。新锁摘要 `f4b79991a8c189110d1ee2f5376c88a34be40a5bb0da8a3dcb016b740057f880`，Node/Miniflare及其他依赖版本不变；没有在服务运行中安装依赖。
- 日志确认新固定版本、PID40518/running、首页200。17张npd表全部字段与本次备份相同，quick_check正常、外键错误0，原8账户/4项目/40阶段和历史保留。原业务附件仍0，本次原件验证是隔离合成资料，不冒充实际业务附件验收。无数据库迁移和公网发布。
- UTC00:23:13公开复查仍4中等/2高，共6个受影响包，requiresAttention=true；审计没有被抑制。公开公告仍无修复版本。disableTypes在识别后阻止尺寸计算，普通图片导入沿用框架0×0兜底，元数据路径明确报错；只证明已验证构建调用路径和样本，不是所有解码器/运行时路径的无漏洞保证。README、安全评估和本地验收清单已同步说明。总体成熟度目标仍未完成，浏览器操作未获授权，其余安全和实际业务验收边界保留。

## 2026-09-07 本地需求复核与阶段附件失败处理

- 复核用户要求：首次空库创建管理员、管理员新建八角色账户、登录退出与会话撤销、节点版本和变更留痕、工艺/采购建项成员、四项电机规格字段以及看板负责人筛选均在当前实现中，未重复初始化或替换现有数据。
- 修复阶段工具栏的附件上传回调只有finally而没有catch的问题。失败提示在项目页持续显示原阶段、文件名和错误原因，切换Sheet不错误归属到新阶段；提醒先核对附件列表再手动重选本机文件，不自动重传。使用同步ref阻止React禁用状态渲染前的重复回调；结束后允许重新选择同一文件。已有登录失效/账号变化提示原文保留，上传成功但刷新失败仍由父工作区说明已上传而非鼓励重传。
- 新增3项 `tests/stage-upload.test.mjs`，在Node中执行实际组件回调，覆盖401文案/版本冲突/网络或未知异常、阶段切换后的归属、并发重复调用及同文件手动重试，纳入默认测试。TypeScript、定向ESLint、diff检查及完整npm test通过（基础60项和各业务专项）。无浏览器/DOM操作，不声称浏览器交互已验收。
- 固定候选 `20260906T235534Z-bcf5e221f2a0` 的完整隔离HTTP回归通过，包括首次管理员、八角色账户、权限与会话撤销、项目/协作版本、负责人交接、设计字段、文件上传下载和原生归档。测试使用全新临时库，不对现有3011项目试写。
- 暂停同一本地托管服务并生成成套备份 `backups/hengda-2026-09-06T23-59-16-876Z-BdUVTw` 后启用该候选，恢复后日志确认新版本、PID37787/running、首页200。只读比对17张npd表全部字段与本次备份一致，quick_check正常、外键错误0；保留8账户、4项目、40阶段及40历史版本。旧错误日志最后修改于9月6日21:26，不是本次启动错误。无数据库迁移、依赖版本/锁文件变化或公网发布。
- 此次完成的是功能复核及附件失败处理补强，不把既有依赖告警、浏览器/真实业务验收或长期运行边界视为已关闭。

## 2026-09-07 依赖升级隔离评估与内嵌风险识别

- 检查官方npm元数据后，仅在 `/private/tmp/hengda-dependency-eval-qBDzhz/candidate` 的独立程序副本安装vinext 1.0.0-beta.9与@vitejs/plugin-rsc 0.5.34，使用ignore-scripts，不含现有D1/R2或备份。副本npm test通过；未启用该候选，没有改变根依赖、锁文件、固定运行版本或业务数据。
- 实际源码检查发现新版只是将image-size 2.0.2内嵌至dist/deps，元数据构建和Worker图片导入仍引用其解析器。内嵌ICNS代码仍存在零长度不前进循环；32MiB/250ms受限子进程中，正常SVG返回，异常ICNS进入解析后超时被终止。故不把npm报告中高等级数量降至0当成修复。完整记录及公告来源见 `docs/dependency-security.md`，此诊断不等同运行时HTTP可达性证明。
- 补强 `scripts/audit-dependencies.mjs`，保留原npm统计，另列内嵌image-size路径、版本、哈希与公告；存在内嵌待处理项时命令也失败。新增两项防误判测试，与原两项审计测试共4项通过；定向ESLint和diff检查通过。诊断探针只显式运行，不进入应用或默认npm test。
- 公开复查UTC23:39:59当前版仍4中等/2高，23:40:01隔离候选npm为4中等/0高但补查识别1处高等级内嵌代码。两者均requiresAttention；未隐藏、修复或接受这些风险。本轮不需重建/切换本地运行版，原固定版本完整性复核通过。浏览器验收授权仍未收到，总体目标保持未完成。

## 2026-09-07 多标签页切换账号防误写

- 隔离HTTP复现：旧管理员页面声明管理员身份、实际请求携带另一个销售账号会话时，旧版本成功保存该销售的看板配置（200），没有检测页面身份与提交身份不一致。这是操作归属一致性缺口，不是绕过既有登录或角色授权。
- 新增 `lib/workspace-identity.ts`。本地 `POST /api/action` 和 `POST /api/files` 在真实会话认证之后、JSON/multipart解析及业务存储之前核对 `X-NPD-Actor` 是否与已认证用户ID一致；不一致或缺失返回409/`ACTOR_CONTEXT_CHANGED`、no-store。此请求头仅是页面身份前置条件，不是凭据，不能替代认证或权限校验；未登录仍401，同账号越权仍403，业务版本冲突仍409。登录/退出和只读请求不新增该条件，托管ChatGPT模式未启用本地强制条件。
- 当前工作区的保存和上传携带界面正在显示的 `me.id`。身份冲突不触发自动刷新/重试；普通业务冲突仍可刷新，但合并前必须是同一用户，拒绝把另一账号的数据/姓名替换进打开的窗口。写入已成功但随后刷新时账号改变，仍提示已保存且不要重复提交，不错误声明写入失败。未新增自动登录、草稿持久化或自动恢复；更新前打开的旧客户端缺少请求头时会被拒绝，需要先保留输入再刷新。
- 新增6项 `tests/workspace-identity.test.mjs`：精确ID、空值/多个值、两个实际路由在解析和存储前拒绝（零D1/R2调用），以及在Node中调用真实工作区回调验证身份冲突不刷新、普通冲突不合并另一账号、成功后刷新失败不重提、上传绑定及同账号正常更新。React状态观察使用测试替身，无DOM或浏览器操作，不冒充浏览器验收。
- TypeScript、定向ESLint、完整npm test通过。固定候选 `20260906T231517Z-60ec3b60f265` 完整HTTP通过：双向账号不一致、缺少页面身份、客户写入与真实文件上传拒绝，工作区数据与审计保持不变；身份头不能代替登录，原八角色、阶段/协作版本、账户撤销、上传下载和Word/Excel/ZIP回归继续通过。100项目规模读取/长历史Excel也通过：四次读取57/51/137/144ms，SSR544ms，Excel45,176字节/66ms；仅隔离样本，不是容量或浏览器速度保证。
- 更新前成套备份 `backups/hengda-2026-09-06T23-17-51-051Z-KUtJaC`，切换候选并恢复原本地服务。启动日志确认新固定版本，PID33039/running，首页200，匿名工作区和概览导出401。17张npd表逐字段与备份相同，quick_check正常、外键错误0，原8账户、4项目及历史数据保留。无数据库迁移、依赖变更或公网发布；README补充使用提示与本地API前置条件。

## 2026-09-07 登录失效响应与未保存内容提示

- 从当前代码确认工作区返回401，但保存、上传、文件下载、历史详情及项目导出将缺失身份当作400/403，概览导出甚至未捕获错误。新增真实HTTP断言在原固定版本复现 `POST /api/action: 400`，不是把正常权限拒绝误判为登录失败。
- 新增 `lib/auth-required.ts`：请求身份守卫抛出专用认证异常，六类业务入口只针对该异常返回401、`AUTH_REQUIRED`及no-store，不解析未认证写入正文、不生成下载文件。权限、版本冲突和其他业务校验保持原有处理；概览导出的非认证异常改为不暴露内部细节的503。工作区原有401保持不变。
- 保存和上传的客户端401提示明确“本次操作未执行”，建议先复制留存输入再登录，并说明刷新/关闭会丢失未保存内容。沿用现有窗口失败不关闭的处理，不自动跳转、不把当前输入写入浏览器持久存储，也不自动重试。保存成功但随后刷新失败仍使用原来的“已保存、不要重复提交”提示，没有混用失败写入提示。尚未新增跨标签页身份绑定或重新认证后自动恢复草稿。
- 新增3项认证异常/错误提示/真实身份守卫单元测试，并纳入默认npm test。TypeScript、定向ESLint、完整npm test通过。候选 `20260906T225806Z-f4ebd13e23cd` 完整HTTP回归通过：六类入口分别覆盖匿名、退出、重置密码及停用后的旧会话，均401/no-store；正常八角色、越权403、版本冲突409、上传与原生Word/Excel/ZIP归档继续通过。全部业务写入测试仅在独立临时库执行，未做浏览器点击验收。
- 更新前备份 `backups/hengda-2026-09-06T23-01-42-500Z-pQnxFs`，切换已验证固定版本后恢复同一本地服务。启动日志确认新版本，PID30913/running，首页200，匿名工作区和概览导出401/no-store。17张npd表逐字段与备份完全一致，quick_check正常、外键错误0；保留原8账户、4项目及全部历史数据。无迁移、依赖升级或公网发布。依赖告警、浏览器验收及正式运行保管安排等未结事项继续保留。

## 2026-09-07 账户、节点版本和新增字段专项复验

- 按本轮要求核对现有实现，不重复搭建账户体系或迁移既有数据。当前固定版本 `20260906T222440Z-353b02d2ea68` 文件校验通过，本地3011首页HTTP 200；没有切换版本或重启业务服务。
- 对该固定版本重新执行 `test:production-runtime`，在随机端口、全新临时数据目录通过首次管理员、八角色开户/登录、退出撤销会话、密码重置/停用、越权拒绝、工艺/采购建项成员、四项规格字段及Word/Excel/ZIP内容验证。成员变更、订单解除后重关联、暂停恢复、负责人往返交接的旧版本请求均被拒绝；现有业务库未用于测试。
- 重新执行 `test:transactions` 全链，通过并发保存、故障回滚、生命周期与负责人交接专项。另重跑看板模型/渲染、版本差异、只读表单、账户迁移及登录请求测试，共15项通过；负责人筛选统一作用于统计和项目范围，版本差异能区分新增/修改/移除而不补造旧快照内容。
- 本轮为既有功能复验和记录更新，没有新业务代码或依赖变更。接口测试及静态渲染不等同浏览器点击验收；依赖告警等剩余边界仍见本地使用验收清单，未宣称零缺陷或正式验收全部完成。

## 2026-09-07 本地交付准备复核

- 当前固定版本仍为 `20260906T222440Z-353b02d2ea68`，本轮重新校验版本文件完整性通过；本地首页200，托管服务running/PID26949。没有重启、切换版本或在真实业务中试写。
- 重跑 `npm run security:audit` 取得有效公开报告，UTC `2026-09-06T22:32:36.903Z`仍为4项中等、2项高等级、0项严重；6是受影响包数量。高等级image-size/vinext，中等esbuild及drizzle-kit相关依赖链。命令以1退出表示发现风险，不是网络失败。修复建议涉及跨版本、预发布或降级，未执行force fix、修改依赖或抑制告警；后续仍需评估可达路径与隔离升级兼容性。
- 修正旧系统蓝图与现状冲突：补齐工艺/采购、首次管理员、负责人交接、四项电机字段、当前设计版次证据和原件核验，去掉电机编码要求及固定近6月趋势；写明原生XLSX/DOCX/含原件ZIP、当前任务口径及本地备份边界。ERP/CAXA等未请求集成改列为可另行确认的扩展，不再暗示已实现或本地使用必需。
- 新增 `docs/local-acceptance.md` 按原需求逐项列出现有实现、可复现专项和证据边界，并给出管理员实际使用顺序。区分接口/静态渲染已有证据与浏览器操作未验收、合成夹具与真实业务资料、项目内备份与异盘保管。没有把旧章节的“浏览器已通过”作为当前版本证据，也未重新声称所有历史测试在本轮重跑。
- 本轮仅文档变更和只读检查，不需重新构建/部署；git diff --check通过。总体目标仍未完成：浏览器操作需明确授权，依赖风险、实际账号/业务资料核对和运行保管边界尚未关闭。不能据现有部分证据宣称“成熟系统正式验收完成”。

## 2026-09-07 我的任务与项目生命周期对齐

- 复核发现旧任务清单仅排除已完成阶段，没有排除暂停或已关闭项目；侧栏独立计算还可能将孤立阶段多计。只按阶段主责角色归集也漏掉工艺/采购已有维护权限的协作阶段；逾期判断使用UTC日期，空日期直接按字符串比较。
- 新增 `lib/task-model.ts`，侧栏与提取后的 `Tasks.tsx` 共用同一模型。使用工作区中的当前账号资料，核对项目可见范围及既有canEditSheet规则；草稿/进行中未完成阶段计入待办，含后续计划。暂停阶段仅按恢复后可维护的岗位范围作只读分类并单列，不算待办；已完成/终止项目不算待办，即使管理员有重开权限。停用/缺失账号、不再参与的项目和孤立阶段不计；管理员、负责人、发起人及协作角色沿用原有权限，不赋予新权限。
- 日期使用既有北京时间工具，计划日为当天不算逾期，缺失/无效日期排末且不判逾期；受阻阶段仍计入待办风险。清单使用稳定排序而不修改输入；人员名沿用统一节点分工模型。“我维护的已完成阶段”按可见阶段当前completed状态及updatedBy计算，明确不是历史完成次数。空状态不再错误断言所有任务均已完成；暂停列表提供查看入口及不自动顺延计划的说明。页面仅做分类，不自动恢复/关闭项目，不修改阶段记录。
- 新增6项 `tests/task-model.test.mjs`：暂停→恢复→终止/完成、草稿、八角色数量、负责人/发起人、停用/转岗/撤销成员/孤立阶段、北京时间午夜、当天/无效日期、受阻、完成口径、输入不变及真实React静态渲染。真实本地HTTP回归新增认证首页侧栏检查：10阶段项目管理员10、工艺5、采购2；暂停后管理员/采购均0，恢复后10/2，终止后均0，阶段数据保留。任务页使用同一模型传参，静态渲染验证暂停独立入口与空状态；未做浏览器点击/布局验收。
- TypeScript、定向ESLint、完整npm test通过；最终父组件以useMemo复用任务模型后重新类型检查、构建并准备候选。固定候选 `20260906T222440Z-353b02d2ea68` 的完整HTTP回归和100项目规模读取/长历史Excel导出回归均通过。业务测试全在独立临时库，不接触原3011数据；没有新增数据库迁移、依赖或公网发布。
- 本地更新前备份 `backups/hengda-2026-09-06T22-28-32-997Z-TcIUrl`，启用上述固定版本后重启。日志确认版本、PID26949、首页200。全部17张npd表全部字段与备份相同，quick_check正常、外键错误0；仍8账户、4项目、40阶段/历史版本，原数据保留。

## 2026-09-07 当前节点分工一致性与失效会话提示

- 复核发现：看板列出岗位多人，项目表格/卡片却只取第一名成员；停用账户未标识，阶段页仅有责任角色。新增 `lib/stage-assignment.ts` 统一当前项目、对应阶段岗位、成员与当前账号的展示口径，应用于看板、表格/卡片和阶段页。人员姓名/角色/启用状态以此次工作区读取的账号为准；同岗位多人完整列出，同名不同账号不合并，重复成员ID不重复，其他项目成员和未分配人员不混入。
- 停用人员明确标注；账号资料缺失时提示待核对；无启用的对应岗位成员时显示待分配及操作指引。阶段提示独立于小屏隐藏的元信息区，使用可换行14px文字。团队区也标识停用成员。未修改已有权限、进度、历史快照、阶段版本或报告数据；该提示不是新的放行条件，也不把负责人/管理员的维护权限当作岗位分配。不是实时推送，其他人的修改需重新读取工作区。
- 新增5项 `tests/stage-assignment.test.mjs`，验证停用/启用/改名/转岗、多个/同名/重复成员、跨项目隔离、缺员/缺账号、阶段切换、输入与历史字段不变及真实React渲染文字转义。扩展看板静态渲染测试，验证多人与停用标识。未进行浏览器点击、DOM或像素验收。
- 扩展真实本地入口HTTP回归，以隔离库实际新建销售、加入项目、停用两名销售、重新启用并改名，检查认证首页返回的当前节点文字和历史详情原文。此测试先暴露旧会话刷新返回503并误报初始化；修复 `app/api/workspace/route.ts`，未认证明确返回401/`AUTH_REQUIRED`和重新登录提示，不读取业务快照。所有工作区响应添加no-store；返回的当前人员资料从同一工作区结果取值，避免沿用前期认证时的旧姓名/角色。该修复不等于已统一所有API的认证错误码，也未添加自动跳转或自动保存草稿。
- 新增2项 `tests/workspace-response.test.mjs` 验证401、零业务读取、禁止缓存和返回最新账号资料。TypeScript、定向ESLint、完整npm test通过，新增响应测试单独执行通过后纳入后续默认test。修复后的候选 `20260906T220652Z-ea5a29db314e` 完整HTTP回归与100项目规模读取/大历史Excel导出回归通过。真实HTTP确认停用旧会话401、重新登录提示、阶段记录与已保存快照原文不变；原有账户、权限、交接、状态版本、附件与Word/Excel/ZIP测试也通过。
- 仅本地更新：暂停保活服务，备份至 `backups/hengda-2026-09-06T22-10-02-221Z-d3FpLd`，启用上述固定版本并重启。运行日志确认该版本、PID24820，首页200、匿名工作区401且no-store。全部17张npd表所有字段与更新前备份一致，quick_check正常、外键错误0；仍为8账户、4项目、40阶段/历史版本。没有新增迁移、依赖变更、业务试写或公网发布，先前未通过完整回归的候选未启用。

## 2026-09-07 历史记录读取与大表 Excel 兼容修复

- 工作区的阶段版本查询改为显式选取元信息，不再从数据库读取随后丢弃的完整历史快照；版本条数、人员权限和同一批读取事务不变。历史详情和项目归档仍按权限读取完整正文。`tests/workspace-history.mjs` 先在旧查询下失败，修复后通过：1,040条历史、32,824,680字节原始快照，数据库返回的版本元信息323,048字节；旧新版本详情/归档正文保持一致，非成员仍被拒绝。
- 新增 `tests/workspace-scale-runtime.mjs`，强制使用全新临时目录，通过编译后的本地Worker执行HTTP读取。100项目、1,000规格、2,000零部件、1,000阶段和1,000历史版本（32,820,000字节正文）的固定夹具，顺序读取加三路并发刷新均通过；本次四次耗时51/51/139/146毫秒，工作区JSON各2,437,326字节。认证首页服务端HTML543毫秒、2,823,503字节。这些是本机隔离样本，不是浏览器加载时间、长期负载或容量保证。
- 该测试实际发现大表导出返回400：`The Worker method is not implemented`。write-excel-file 4.1.1 的ZIP实现对超过约160KiB的单项调用异步Web Worker，而workerd不支持。构建中仅将该库的ZIP入口定向到 `lib/xlsx-zip.ts`，使用已有fflate的同步压缩并保留Promise接口；未改node_modules或锁文件，也未截断行数/历史数据。其他依赖的压缩方式不受影响。同步压缩占用当前请求线程，超大单项目仍需另行测量内存与CPU。
- 候选 `20260906T214104Z-7dadbed74fea` 的规模HTTP测试通过：历史工作表XML明确超过160KiB，导出ZIP逐项CRC校验通过，10个阶段的长JSON（含中文、XML特殊字符和emoji）从Excel分段逐字符拼回原文。该项目Excel45,159字节，下载加内容验证66毫秒。质量成员只获得所属1个项目，对另一个项目历史详情返回403。夹具只验证读取体量，不冒充完整审批流程。
- TypeScript、定向ESLint和完整 `npm test` 通过；同一候选的 `test:production-runtime` 完整HTTP回归通过，含首位管理员初始化、八角色开户/登录/登出、重置停用撤销会话、权限、看板偏好、负责人交接、并发版本、上传下载及XLSX/DOCX/ZIP归档。未进行本轮浏览器交互或版式验收。
- 更新前暂停本地保活服务并生成备份 `backups/hengda-2026-09-06T21-43-56-480Z-pgdxRn`，随后启用上述已验证固定版本。重启后PID20830、日志确认新版本、3011返回200。只读核对全部17张npd表的全部字段与备份一致，quick_check正常且无外键错误；仍为8账户、4项目、40阶段/历史版本、0业务附件。无新增数据库迁移、无公网发布。先前失败候选未启用，未删除旧版本或现有业务记录。

## 2026-09-07 负责人交接闭环

- 现有项目只能建项时指定负责人，补齐项目概览“项目团队→负责人交接”。发起人、当前负责人、管理员可办理；完成/终止项目仅管理员。接任人限启用的销售/设计/管理员，需填写新旧职责、原因和未完成事项。暂停期间允许交接但不恢复项目；原负责人停用时管理员无需重新启用旧账户。沿用Sites现有数据/权限约束，仅本地更新。
- 新增独立ownership_version；交接窗口固定负责人、状态版次、交接版次及两名相关成员的记录版次。交接、两人职责/成员版本、负责人版本及带时间戳审计同一事务；缺版、旧窗口、A→B→A、同一请求并发、成员职责中途变化或目标账户提交前停用等均拒绝，最后一步失败整体回滚。新负责人已有职责在窗口保留供确认，不静默覆盖。原负责人保留成员身份，发起人和管理员不因交接失去各自权限。
- 工作区/归档按owner_id关联当前姓名，看板负责人及其筛选使用同一字段；新负责人立即获得项目访问和维护权限。发起人、阶段责任角色、已保存表单/阶段版本和历史试验/检验人员不改写。团队区明确当前负责人并对其停用作出提示。审计保存前后姓名/ID、交接版本、职责及成员版本和原因。
- 阶段请求从前期授权到读取版本上下文校验生命周期及交接版本，并在提交时再次比对；交接后即使原负责人仍是普通成员，不能用旧的负责人权限完成已经在途的阶段写入。这不等同撤销所有角色权限，亦不是所有业务窗口的全局锁。
- 生成并检查 `0009_brave_cloak`，只追加常量默认1的ownership_version列；已应用迁移不修改。迁移测试确认所有旧项目字段及lifecycle_version保留、失败回退后可重试、重启不重置。旧项目从交接V1开始，不补造过去的交接历史。
- `tests/project-ownership.mjs` 及独立、不持久化真实D1入口通过上述冲突/权限/回滚、暂停交接、原负责人停用后管理员交接、终止项目权限及状态不变等场景。`tests/owner-transfer-render.test.mjs` 用真实React静态渲染验证窗口基线、职责、角色候选、空候选状态、文字转义及禁用提交不是加载态；不是浏览器交互或像素验收。
- TypeScript和定向ESLint通过，完整npm test（33项基础及各专项，新增2项交接迁移/渲染和业务场景）通过。真实D1生命周期与空白部署/重启回归通过，D1/R2恢复演练通过。候选固定版完整HTTP登录、越权、上传和原生XLSX/DOCX/ZIP回归通过；新增交接HTTP验证普通成员403、接任人立即访问导出并继续交接、发起人及阶段历史不变、往返后旧版409且项目/成员/审计不变。
- 候选 `20260906T211110Z-7c1ac44a84cc` 校验并通过上述HTTP验收后，先确认旧服务HTTP200，再暂停原托管服务，生成备份 `backups/hengda-2026-09-06T21-15-07-673Z-JYFTgl` 并激活。恢复后日志确认该版，首页HTTP200、服务running（核验PID17419）；0009实际应用于UTC 2026-09-06 21:15:08。
- 只读比对17张业务表的所有旧字段与本轮备份一致；4项目交接版本均为1，8账户、26成员、40阶段及40历史等保留。quick_check=ok，外键问题0。未在真实业务中办理任何交接、停用人员或修改职责。Word/Excel布局本轮未改，实际业务附件数仍为0；附件恢复结论来自隔离测试。
- 总体成熟度目标未完成：浏览器操作验收、规模/长期运行、上游依赖告警及其他人员/项目维护边界仍保留。交接功能不包含人员离职批量撤权、审批签章、自动通知或历史报告替换，不应据此声称整个系统已无缺陷。

## 2026-09-07 项目状态往返与阶段提交保护

- 复核发现项目状态旧窗口及阶段事务只比较当前状态值，暂停后恢复为active可形成ABA窗口。新增 `npd_projects.lifecycle_version`：显式暂停/恢复/终止递增，阶段聚合导致自动完成或管理员重开时也递增；仅进度变化不递增。状态窗口保存打开时的版本，状态API对缺失/无效/旧版请求返回冲突，事务提交时再核对一次。状态、版本与原因审计原子保存，审计包含前后状态版本。
- 阶段读取上下文保存当时的状态版本，提交前发生暂停/恢复往返即拒绝；共同提交守卫同时复核当前有效账户、角色、项目访问范围和已关闭项目的管理员限制。阶段原版本机制继续生效，状态变化不伪造或重置阶段历史。本项不是所有旧编辑窗口的统一项目锁，成员职责/订单等仍使用各自版本，不宣称所有业务操作均接入生命周期版本。
- 按Sites迁移/权限约束生成并检查 `0008_fuzzy_gressill`，仅新增常量默认1的状态版本列；空库兼容前置创建旧项目表（含编号唯一及原外键），已有表不覆盖。旧迁移未改。旧项目升级为V1，不补造过去的状态版本；迁移失败后列与记录整体回退、成功重试、重启保留版本及编号唯一由独立SQLite测试验证。
- 新增 `tests/project-lifecycle.mjs` 和无持久化真实D1入口，两者通过：阶段/状态事务提交前实际注入暂停恢复ABA、缺版/无效版本零写入、成员撤权后阶段保存拒绝、自动结项/重开递增及普通进度不递增、归档读取状态版本。最终阶段聚合测试使用合成前置完成数据，不是全部报告逐阶段放行的验收。原并发事务测试继续验证相同暂停双提交只成功一次和末步故障零审计。
- TypeScript与定向ESLint通过；完整npm test通过（33项基础检查与所有专项，另含新增状态迁移/生命周期用例）。真实D1事务回归及空白初始化/重启通过。固定候选 `20260906T204504Z-90afe36e7c6c` 的完整隔离HTTP账户、上传、原生XLSX/DOCX/ZIP回归通过，并实际验证状态往返后旧版/缺版终止请求409，项目/审计/阶段历史不变。固定版本覆盖来源和重启检查通过。备份运行测试原简化项目表与新迁移冲突，调整仅测试夹具使用迁移生成的项目结构后重跑通过，恢复状态版本7和附件原字节均保留。
- 采用固定版本流程，构建及候选验收期间原服务HTTP200、PID11747未切换；只在最终备份/激活阶段短暂停止同一托管服务。备份 `backups/hengda-2026-09-06T20-50-42-261Z-PFG40u`，激活上述候选后日志确认版本，首页HTTP200、服务running（核验PID14737）。0008实际应用于UTC 2026-09-06 20:50:43。
- 只读核对17张业务表所有旧字段与本轮备份一致；4项目新增状态版本均为1，8账户、40阶段/历史等全部原数据保留，quick_check=ok、外键问题0。没有在实际业务库执行测试暂停、终止、成员删除或表单改写。Word/Excel布局本轮未改，状态版本窗口文字未做浏览器交互或像素验收。
- 总体成熟度目标仍在进行中：浏览器验收、规模/长期运行、上游依赖告警及其余领域的复杂维护边界仍需完成，不能将本次状态/事务证据扩展为全部场景零缺陷的承诺。

## 2026-09-07 固定本地发布版本与升级隔离

- 遵循现有Sites部署/存储约束，仅本地部署。新增 `scripts/local-release.mjs`：准备独立版本目录，记录Worker、静态资源、运行脚本、迁移、包清单的SHA256和大小，检查后只读封存。准备与激活分开，激活串行锁与原子指针切换；已启用固定版本后缺失/损坏指针拒绝启动，不退回未验收dist。数据库及附件仍使用原业务目录。
- `scripts/local-server.mjs` 默认加载已激活版本及其运行/迁移脚本；显式 `--build` 只供当前构建验收，`--release` 可独立验收候选版本。核对Node、Miniflare版本及依赖锁摘要，阻止存储绑定改变、旧迁移重写及缺失已应用迁移的版本切换。不自动重启、清理旧版本或降级数据；详情见 `docs/local-releases.md`。
- 新增3项发布单元测试通过：准备不激活、资源封存及篡改/符号链接/路径拒绝、缺指针失败关闭、运行环境不匹配、新环境候选激活、存储/迁移保护及并发激活。隔离真实运行时测试实际覆盖构建资源、当前运行脚本及迁移来源，运行中页面/CSS与重启后仍一致；所有破坏性注入仅在测试专用临时目录进行。
- TypeScript及定向ESLint通过；完整 `npm test`（33项基础检查及业务、并发事务、迁移、归档、原生导出、备份、登录专项，含上述3项发布测试）通过。当前构建与候选固定版本分别通过完整隔离HTTP账户、权限、上传下载、XLSX/DOCX/ZIP及成员/订单旧版冲突验证。真实D1/R2备份恢复和隔离发布覆盖/重启验证通过。
- 更新前备份 `backups/hengda-2026-09-06T20-21-08-531Z-j5sFiw`。首次激活 `20260906T202129Z-e89b407d26e4` 后恢复原LaunchAgent；日志确认使用该固定版本，本地首页HTTP200、服务running（核验PID11747）。只读核对17张业务表全部字段与备份一致，8账户、4项目、7规格、40阶段/版本等原数据保留；quick_check=ok，外键问题0。原业务附件数仍为0，不以隔离附件测试冒充真实业务附件验收。
- 限制：仍共享安装的node_modules和宿主系统，未冻结整个运行环境或哈希每个依赖文件；只读权限不是本机管理员安全边界。当前引导/版本选择脚本仍属于项目源目录。业务备份不包含软件版本，需另保留代码/版本目录和依赖；没有承诺断电持久性、跨运行时数据降级或操作系统重启验收。
- 本项解决普通重建覆盖在用资源的问题。浏览器交互验收、规模/长期运行与上游依赖告警仍待处理；不宣称所有复杂场景或总体成熟度目标已全部验收。

## 2026-09-07 成员职责与订单关联独立版本

- 沿用Sites数据及权限规范，为成员记录和销售订单新增独立递增版本。成员编辑窗口保存打开时的记录ID/版本（新增为显式null）；订单关联窗口保存原关联及版本。改为B再改回A、同秒相同内容双提交、遗漏版本等不再只靠值/时间判断，旧窗口拒绝覆盖，输入仍保留。同一项目不同成员使用不同记录版本，不要求全队互锁。
- 建项时选单也提交窗口打开时的订单版本，在读取及创建事务内复核；创建项目关联订单、单独关联/解除都递增订单版本。成员/订单修改、版本及审计同事务；审计写入前后版本和职责/关联变动。订单提交事务同时复核当前项目访问范围和维护权限，不仅核对早期缓存的角色。
- 新增生成并检查的迁移 `0007_abandoned_mandarin`，只给npd_project_members和npd_sales_orders增加常量默认1的version列。兼容空白本地库先创建原有表结构，已有表不覆盖；旧迁移不修改，已纳入本地增量执行清单。迁移失败回退/重试、全部旧字段保留、重启不重置版本均由独立SQLite测试通过；正式空白本地入口与真实D1空库/重启也通过。
- 新增 `tests/collaboration-versions.mjs` 及不持久化真实D1入口：成员/订单A→B→A旧窗口、缺失版本零写入、相同并发只成功一次、建项旧选单拒绝、正确版本建项递增、工作区版本与审计一致均通过。现有故障注入事务用例已补充窗口基线，成员/订单末步故障回滚继续通过。
- 完整 `npm test`（33项基础检查及各专项，另含新迁移测试和协作版本场景）、TypeScript和定向ESLint通过；正式入口HTTP验证成员旧版409、订单关联后解除仍拒绝旧版、失败零审计，既有账户/上传/三种导出/登录限速回归通过。真实D1协作、事务及空库初始化/重启再次通过。尚未做浏览器窗口点击验收，不能把服务端测试描述成浏览器交互验证。
- 更新前备份 `backups/hengda-2026-09-06T19-56-03-294Z-4jmu6S`，暂停同一托管服务构建/验收后自动恢复。本地首页HTTP200、服务running（核验PID9146），0007实际应用于UTC 2026-09-06 19:56:41。17张业务表所有旧字段与备份一致；26条成员和5张订单新增version均为1，8账户/4项目等原数据保留，quick_check=ok、外键问题0。未删除、重写职责、改变订单关联或补造升级前版本。
- 本次补齐下方历史记录中的成员/订单长窗口版本缺口，不代表自动合并、任意历史回滚或所有业务对象都已有独立版本。原有Word/Excel布局未改，当前控制版本通过接口和操作日志追溯。浏览器验收、冻结构建升级、规模/长期运行与上游依赖告警等仍待完成；总体成熟度目标保持进行中。

## 2026-09-07 试验/质量阶段放行原件核验

- 发现原阶段门只以附件索引判断有无报告，未检查存储对象。现试验、质量阶段标记完成前，仅核验当前设计版次下最新有效试验类别/整机及零部件检验记录对应的原件；被新报告替代的旧原件缺失不影响当前放行。核验项目/阶段归属、受控对象键、正整数长度、R2存在性、大小和ETag，缺件/不符/存储故障拒绝完成。
- 放行事务额外核对已检查的附件索引仍存在且对象键/大小/文件名未变；原有全项目阶段版次保护继续拦截核验期间的设计变更。失败不写入阶段、审计或版本；成功的阶段版本保存 documentId、fileName、size、etag、checkedAt，核验元信息不额外复制内部对象键。
- 历史版本详情新增“本版本放行原件核验”，展示原件名称、字节数、时间和校验标识；旧版本/损坏/重复或不完整凭据不被当作成功核验。原始快照随项目Excel归档。自动进度说明明确是报告记录和附件索引齐套，放行时还要核验原件。
- 沿用Sites数据与附件存储规范，仅本地更新，未加表/迁移/新依赖。此项是放行当时的存在性及大小核验，不是内容检验、病毒扫描、数字签章、持续监控或D1与R2跨系统事务；文件在核验后被绕过系统外部删除/替换，不能由此次检查完全阻止，历史状态也不会因此自动变更。没有回填或重置旧阶段。
- `tests/stage-originals.mjs` 与真实不持久化D1/R2的 `tests/stage-originals-runtime.mjs` 均通过：两阶段、整机及零部件、缺件、错长度、空件、越界对象键、存储故障、旧报告替代、索引/设计并发冲突和成功凭据。真实测试实际上传/删除隔离R2对象，业务库不参与。原有集成用例显式使用元数据测试替身，不把替身当作R2证据。
- 完整npm test（33项基础及业务/归档一致性/新原件核验/事务/空库/原生导出/备份/登录专项）、TypeScript、定向ESLint通过。正式构建隔离HTTP账户/跨站写入/上传下载/三种导出回归、真实D1事务回归及真实原件核验再次通过。新核验凭据的模型测试通过；浏览器版本窗口点击和像素验收尚未执行，不作浏览器已验证的声明。
- 更新前备份 `backups/hengda-2026-09-06T19-36-59-155Z-aCBIe1`；暂停同一托管服务完成构建/验收后自动恢复，首页HTTP200，服务running（核验PID6876）。17张业务表全部字段与备份一致，8账户/4项目等原记录保留，quick_check=ok、外键问题0。实际附件库仍无业务附件，原件结果来自隔离测试。
- 总体目标仍未完成。浏览器验收、成员/订单长时间编辑窗口的独立版本、冻结版本升级、规模及长期运行与上游依赖告警等继续保留；下方历史记录中“阶段放行时实际附件存储核验”缺口现由本节补齐，不意味着所有阶段和所有异常场景均已穷尽。

## 2026-09-07 一致时点读取实际验收与本地更新完成

- 上一轮审核超时未启动的 `test:archive-snapshot-runtime` 本轮已成功执行。独立、不持久化的真实本地D1通过同一次13组归档读取、读取前后立即修改、并发修改时记录同版、成员移除/停用/管理员降权后使用旧用户对象仍拒绝，以及工作区15条单次读取。此结果补齐上一节仅SQLite适配器测试的证据，不是浏览器验收。
- 暂停同一托管服务后生成备份 `backups/hengda-2026-09-06T19-21-53-666Z-hUUHqE`；完整 `npm test` 通过（32项基础检查及业务、归档一致性、事务、空库、原生导出、备份、登录专项）。TypeScript与定向ESLint通过，额外真实D1事务回归通过。
- 重新构建后的正式本地入口隔离HTTP测试通过：八角色开户/权限、登录退出、密码重置/停用撤销会话、跨站写入拦截、上传下载、XLSX/DOCX/ZIP及越权拒绝。ZIP清单的 capturedAt、单事务一致性标记及读取时间不晚于生成时间断言通过；三份隔离R2附件原字节和包内Word/Excel格式仍通过。本轮没有修改Word排版，不重复声称新的版式或浏览器交互QA。
- 服务自动恢复，本地首页 `http://localhost:3011/` 返回200，托管服务running（核验PID5122）。只读比对17张业务表的全部字段与本轮备份相同：8账户、4项目、7规格、40阶段、40版本保留，quick_check=ok、外键问题0。没有把合成测试记录写入业务库；真实附件数仍为0，附件结论来自隔离验收。
- 本次遵循Sites现有数据/权限规范，仅更新本地，不发布外网。下方“源码已完成，运行验收及部署待续”的状态是上一轮历史记录，现已由本节补齐。
- 总体成熟度目标仍未完成：浏览器操作验收尚待授权，成员/订单长时间编辑窗口的独立版本、冻结版本升级、阶段放行时实际附件存储核验、规模与长期运行、上游依赖告警等仍需继续处理。本次保证结构化记录的同次读取，不等同D1与附件存储的跨系统事务、数字签章、历史对象版本恢复或已开始下载后的自动撤回。

## 2026-09-07 一致时点读取（源码已完成，运行验收及部署待续）

- 复核发现单项目归档原先先读取工作区，再另查完整日志/版本；工作区内部也用 Promise.all 分别读取多张表，存在多人修改期间新旧内容混读的窗口。现将工作区15条查询改为同一次 D1 batch；权限过滤使用该事务中读取的账户，而不是请求早期缓存的角色，并拒绝已停用账户。
- 单项目归档改为13条同次只读事务查询，不再扫描整个工作区。每条查询以同一个受权项目 CTE 限定范围，在数据库内检查当前账户启用状态、管理员/发起人/负责人/成员关系。完整日志无300条截断，历史快照保留；当前表及全部历史来自同次读取。无模式迁移或新依赖。
- 归档数据增加 capturedAt 和事务内的导出人姓名；ZIP说明/清单区分数据读取时间与文件生成时间，并声明 d1-single-read-transaction。正常附件上传使用独立UUID对象键；附件字节不属于数据库事务，仍依赖索引、存在性/长度预检和条件读取。不能保证绕过系统直接替换存储对象后恢复历史字节，也不保证已开始下载会因随后撤权而自动中断。
- 新增 `tests/archive-snapshot.mjs`：SQLite适配器中通过单批只读检查、读取前/后立即更新的边界、并发更新时13组记录同版、项目范围、成员移除/账户停用/旧管理员对象拒绝，以及工作区15条单批查询。适配器现在能正确返回SELECT结果；测试不再把SELECT作为无返回的run处理。
- 6项ZIP测试（含160MiB增量消费及读取/生成时间分离）、业务/事务/空库/原生XLSX及DOCX/备份/登录专项、TypeScript与定向ESLint通过。仅数据查询和ZIP说明改变，未更改Word排版逻辑，未进行新的浏览器或页面图片验收。
- `tests/archive-snapshot-runtime.mjs` 已准备为不持久化的真实本地D1验收；首次调用和允许的一次重试均因自动权限审核未在期限内完成而未启动，不能把它记作测试通过。正式HTTP用例新增清单读取时间和一致性断言，但本轮尚未重新构建/执行。不能把上一轮正式运行时通过推广到本轮源码。
- **本轮未停止/重启服务、未重新构建dist、未部署上述修改。** 运行版仍为上一节已部署的ZIP版本，业务数据未写入；待实际隔离验收通过，再按暂停同一托管服务→备份→完整构建/回归→重启流程更新。总体目标继续进行，不能据此声称正式一致时点归档已上线。此前待办中跨表读取缺口只完成源码修复，运行验收仍是未完项。

## 2026-09-07 完整项目离线归档 ZIP

- 项目详情新增“离线归档 ZIP”，包含现有原生程序 Word、完整项目 Excel、所有已登记附件原件及附件清单。文件按阶段归类、编号；清单保留原文件名、版次、规格、关联记录和上传人/时间，中文重名不会相互覆盖。原 Word 按钮改称“程序 Word”，不改变原生文档生成逻辑或此前版式验收范围。
- 沿用 Sites 附件存储和权限规范：先验证项目访问权限，再逐一检查附件归属、R2 存在性、大小和 ETag。读取时使用条件请求、核对实际字节数；缺失或变化会拒绝/中止归档，不输出看似完整的 ZIP。清单不泄露内部对象键。下载响应 private/no-store、nosniff，使用本地 Workers 的 FixedLengthStream 提供实际 Content-Length。
- 将已有 fflate 0.7.5 显式声明为直接依赖，使用浏览器/Worker 入口和 STORE 流式打包。附件逐个读取，具备背压和取消处理；Word/Excel 仍先在内存生成，不能宣称全链路恒定内存。采用标准 ZIP32 边界检查，不支持超过 4GB 或文件数超过 65535 的单包。
- 完整 `npm test` 通过：31 项基础检查及业务、事务、空库、原生导出、备份和登录专项；TypeScript、改动文件定向 ESLint 通过。新增单元测试覆盖中文重名/不安全文件名、缺件/大小错误/跨项目、ETag 变化、截断与超长读取、取消和路径边界。160MiB 合成附件增量解包校验通过，此项在 Node 执行，不代表 Worker 最大内存/长期运行压力验收。
- 正式构建的独立本地 D1/R2 HTTP 验收通过：首次管理员、八角色账户权限、会话撤销和登录限速等回归，以及 ZIP 响应长度/CRC、内含 Word/Excel 原生格式、三份上传原件逐字节一致、同名中文文件分别保留、匿名和非成员拒绝下载。未使用真实业务数据做写入测试，未开展浏览器交互验收。
- 更新前备份 `backups/hengda-2026-09-06T18-57-13-918Z-hFP4sO`，暂停同一托管服务构建/隔离测试，结束后自动恢复。本地首页 HTTP 200；17 张业务表全部字段与备份一致，8 账户/4 项目/7 规格/40 阶段/40 版本保留，quick_check=ok、外键问题 0。真实库目前附件数为 0，附件验证来自隔离测试。
- 归档未加密，不等同数字签名、正式签章或数据库备份，也不支持直接导入恢复。跨表一致时点读取尚未实现，正式归档应暂停项目维护。浏览器授权仍待用户回复，成员/订单长窗口独立版本、冻结版本升级、超大规模及长期运行、上游依赖告警仍是未完项；总体成熟度目标继续进行。操作及边界见 `docs/project-bundle.md`，旧章节中“附件原件整包未完成”以本节为准。

## 2026-09-07 看板筛选、日期与统计口径统一

- 本轮按看板建设/数据质量/可视化技能核对数据源、粒度、统计范围和零值表达，沿用Sites规范保留现有本地页面，不创建另外的报表站点。新增 `lib/dashboard-model.ts` 作为卡片、状态分布、区间流量和重点项目的共用计算入口；详情见 `docs/dashboard-metrics.md`。
- 负责人/年度/半年度/月度/自定义区间统一作用于图表，取消固定近6月窗口。计划相交项目之外纳入区间内已开始且截至今日仍未关闭的跨期项目，避免进入新月后旧逾期项目消失。显示当前状态，不冒充历史月末快照。流量只计选定项目在区间内的登记/完成日期；登记时间明确为系统建档而非真实纸质立项。
- “按期项目”改为实际日期≤计划日期的“按期完成”；“未结逾期”排除完成/终止，暂停不自动顺延。该逾期规则也用于后端工作区，保证列表与看板不再把终止项目累计为逾期。平均进度排除终止，空分母显示—；未结高风险排除已关闭项目。
- 统一北京时间，处理UTC跨月、闰年、非法月份/缺失日期/反向区间；缺失或未来实际完成日期显示待核对而不算按期/完成流量。长区间合并连续月份至不超过24组，标注起止范围不截断。零值柱高度为0，有数字及精确明细表；同岗位多名节点责任人全部显示。取消自定义配置不再立即改变主界面指标，保存失败提示并保留选择。
- 偏好读取对历史损坏数据安全回退，保存时前后端验证有效日期、非空/不重复/已知指标；保存与审计同事务，账号启用/角色在提交时再次校验。未新增业务表或改写迁移。偏好本身仍是后保存覆盖先保存，不宣称具备长时间窗口的独立版本历史。
- 只读核对当前V2数据：2026年度4项目/7规格/当前完成1/按期完成1；2026年9月3项目/6规格/登记0/完成0；状态构成与项目总数一致，计划无效/缺失完成日期/登记日期异常均0。4条现有项目建档时间集中于8月29日，早期演示/补录数据不能作为企业真实研发趋势证据。
- `npm test`完整通过（26项基础检查加业务/事务/空库/导出/备份/登录专项），TypeScript和定向ESLint通过，独立真实D1事务与正式本地入口HTTP验收通过。新增6项模型/React静态渲染测试及偏好失败回滚/非法HTTP请求零审计。此处不是浏览器交互或像素验收；已向用户异步请求仅用隔离合成数据的浏览器验收授权，尚未获得回复，不据此宣称可视化最终QA完成。
- 更新前备份 `backups/hengda-2026-09-06T18-31-00-980Z-U260Vr`，暂停同一托管服务构建/测试后重启。本地首页HTTP200、服务running；17张业务表逐字段与备份相同，8账户/4项目保留，quick_check=ok、外键问题0。
- 总体成熟度目标未完成。除浏览器验收外，成员/订单跨长时间窗口的独立版本、完整附件原件包、导出一致时点、冻结版本升级、长期运行和上游依赖告警等仍需继续；本次看板修正不等于全系统无缺陷。

## 2026-09-07 账户独立版本与本地需求复验

- 沿用 Sites 开发规范，仅更新本地服务，保留原系统和现有数据。账户新增独立递增版本，编辑窗口原有基线自动携带该版本；服务端读取和事务提交时均核对。资料 A→B→A、同秒密码重置、相同内容并发保存、遗漏版本请求不再仅靠秒级更新时间判断，冲突零写入并保留界面输入。
- 账户审计增加前后版本以及姓名、邮箱、部门、角色、启停状态的前后值；密码只记录重置事件，不记录明文、盐或哈希。密码/权限变更仍撤销旧会话；普通登录不增加资料版本。没有实现账户历史内容自动回滚，也不能补回升级前未记录的资料。
- 新增生成后检查的迁移 `0006_slimy_gamma_corps`，以常量默认值1添加账户版本列；兼容空库先创建既有账户表结构，不在请求初始化中新增/修改该列。旧迁移未改写，新迁移已纳入本地启动清单。隔离测试覆盖既有账户凭据及资料保留、迁移失败回滚/重试、重复启动不重置版本。
- 全量 `npm test`（20项基础测试及业务/事务/空库/导出/备份/登录专项）、TypeScript、改动文件定向ESLint通过；独立真实D1事务和D1/R2备份恢复演练通过。正式构建隔离HTTP实测重置密码后版本递增、旧账户窗口409且资料不变；同时复验管理员初设、八角色开户登录、非管理员开户拒绝、退出/停用撤销会话、建项工艺/采购成员、四项规格字段、十阶段/十基线及XLSX/DOCX下载。不是浏览器点击验收。
- 更新前备份 `backups/hengda-2026-09-06T18-03-52-916Z-sFALP3`；暂停同一托管服务完成构建和隔离HTTP验收，随后重新启动。首页HTTP200、服务running。只读比较17张业务表的全部旧字段均与备份一致，8账户/4项目保留，既有账户版本均为1；quick_check=ok、外键问题0，新迁移记录存在。
- 本次用户要求逐项复核：首次部署管理员及管理员开户、登录登出已验证；节点版本/原因/时间/前后内容比较和反复变更的已覆盖场景见事务测试；工艺/采购成员与四项规格字段已持久化验证，具体规格界面无电机编码；负责人筛选已有接入。尚未开展浏览器交互验收，不能声称所有复杂反复场景无缺陷。看板近6月趋势与全局统计周期尚未统一、成员/订单跨长时间窗口的更强版本标识、附件原件整包、跨查询一致时点导出等仍为未完项；总体成熟度目标未完成。

## 2026-09-07 原生 Word 归档接入与本地复验

- 项目及单Sheet归档入口正式接入原生 `.docx`（docx 9.6.1），采用正确MIME、文件扩展名、nosniff及private/no-store；非法Sheet（含`__proto__`）拒绝。下载仍先校验项目访问权限，未登录及非成员不能下载。Excel入口不变。
- 档案保留十类原程序表单的当前字段、三类规格/零部件/报告明细、工艺/采购等成员、订单、全部项目操作日志，以及每阶段版本号、修改摘要、原因、操作者、时间和当时节点。保留旧表单未知字段、零值、布尔值及多行文字。历史原始快照仍在项目Excel中，不以摘要替代快照。
- 文档技能的逐页检查促成独立阶段目录、分规格分页、修复空附件索引独占空白页、版本抬头与摘要相邻、短操作日志接续订单页。合成完整项目29页、验证阶段3页、空质量阶段2页，共34页均使用标准文档渲染流程并逐页查看原始分辨率；中文、长附件名、表格和页码无已发现的缺字/裁切。独立python-docx读取全部段落/单元格通过。不是Word桌面交互验收或原纸质表单的逐像素复刻。
- 完整 `npm test`、TypeScript、定向ESLint、正式入口隔离HTTP测试及真实D1事务测试通过。HTTP测试重新验证空库管理员设置、七类普通人员开户登录、普通人员不能开户、密码重置/停用撤销会话、正常退出、工艺/采购成员及四项规格字段持久化、十阶段/十基线、上传下载、原生XLSX/DOCX与越权拒绝。事务测试覆盖重复/过期提交、失败回滚、暂停恢复、成员职责和订单关联等；不宣称穷尽所有反复业务场景。
- 本轮备份 `backups/hengda-2026-09-06T17-25-48-643Z-Ur5CLL`。构建及隔离验收期间暂停同一托管服务，结束后重新加载。首页HTTP200，服务running，17张业务表计数与备份一致（8账户、4项目、7规格、40阶段、40版本）；quick_check=ok，外键问题0。未重置现有管理员、删除旧项目或录入真实测试记录。
- 限制：Word只有附件索引，不内嵌或打包附件原件；实际库目前附件数为0，附件内容验证来自隔离测试。原生Word不等于签章审批、完整附件离线包或全系统最终验收。全面浏览器验收、冻结版本升级、账户更精确并发版本、跨查询一致时点归档及超大规模性能、上游依赖安全告警等仍沿用未完项；总体目标继续进行。

## 2026-09-07 本地账户入口与本次需求复核

- 项目详情增加当前登录姓名、岗位及直接退出入口；本地退出仍为同源POST。异常页“切换登录账号”修复为本地退出，不再把本地人员带到外部身份入口。退出响应禁止缓存；登录错误增加可访问提示，并明确忘记密码由本地管理员处理。
- 复核本次要求：默认空库首次创建管理员、管理员开通其他人员；工艺/采购岗位参与创建项目；具体规格保留出线形式、防护等级、绝缘等级、冷却方式而不显示电机编码；看板已有项目负责人筛选；十个阶段有基线及后续版本、修改原因、操作者和时间。现有库已经初始化，未重新开放管理员注册或删除旧数据。
- 新增实际组件渲染测试，验证退出/切换账号均生成POST表单；扩展独立正式运行时HTTP测试，核对退出清Cookie/no-store，以及新项目工艺/采购成员、四项电机字段、十个阶段和十条基线版本确实保存。
- 完整npm test、TypeScript和定向ESLint通过；独立正式运行时登录/八角色/上传下载/XLSX/限速测试与真实D1事务测试通过，包含重复提交、过期版拒绝、失败回滚、暂停恢复及账户撤销会话。此处不是浏览器交互验收，也不代表穷尽全部反复变更场景。
- 更新前备份：`backups/hengda-2026-09-06T16-45-52-361Z-PhOkRY`。更新期间主动暂停同一托管服务，测试结束重新加载，避免构建时旧程序混用新静态资源；本地首页返回200。更长期的冻结版本升级机制仍未实现。
- 原生Word相关实验文件暂未接入正式导出入口，不能将其视为已交付功能；完整附件归档等未完项沿用下方记录。总体成熟度目标仍在进行中。

## 2026-09-07 登录限速、凭据竞态与增量迁移

- 新增 `npd_local_login_limits`：数据库原子预留尝试次数，每账户10次/15分钟、本机60次/分钟、首次设置5次/15分钟。成功和失败都计入，非“失败10次之后再计数”；到期重新开放，已有会话不受影响。账户桶使用归一化邮箱SHA-256，不保存邮箱明文、密码或请求内容；1小时以前的桶在后续尝试时清理。仅适配当前回环地址本地部署，不信任转发IP头，不宣称具备公网级抗拒绝服务能力。
- 密码验证后，会话、最后登录时间及成功日志原子提交，并再次比对当前启用状态、邮箱、盐和哈希；验证期间停用或改密码不能生成会话。未知/停用/未设密码账户使用统一失败信息和同样的密码哈希工作。正常登录不再修改账户资料的updated_at，避免无关登录引起管理员维护冲突。
- 登录输入限制8KiB，校验实际流字节而非仅信任Content-Length。超长请求413；限速429并包含Retry-After、no-store。浏览器接收中文返回登录提示页，程序请求接收JSON；错误页文案转义，不回显凭据。
- `0005_new_quasimodo.sql` 由Drizzle生成，已检查只新建限速表及到期索引；未改历史迁移。日常启动和测试工具使用独立迁移执行器，记录校验值、事务提交迁移和记录；失败回退，重复执行不重建数据，已应用文件漂移时拒绝启动。没有在业务请求初始化中加入这个表的CREATE语句。历史0000～0004旧初始化仍待后续统一，不能把这次增量机制当成全量迁移重构完成。
- 新增单元/内存SQL测试覆盖20路并发恰好10次进入密码校验、邮箱归一、模块重启仍限速、窗口到期恢复、未知账户/全局上限、已登录会话保留、过期清理、首次设置换邮箱不能绕过、迁移中途失败/校验漂移、密码验证后停用/改密码无会话和无成功日志。独立真实D1的并发/到期测试通过。
- 完整常规测试、类型及定向lint通过；正式本地入口的HTTP测试通过八角色账号、登录、上传、下载、XLSX、429中文/JSON等待提示及413大请求拦截。一次测试使用了账户修改前的旧版本，被409正确拒绝；测试现按界面工作方式刷新后再修改。超长请求处理曾出现连接重置，改为限制实际读取后拒绝，复测返回413且后续正常工作区仍可访问。
- 更新前备份 `backups/hengda-2026-09-06T15-59-09-388Z-l8EC9o` 完成，本次托管备份自动重启成功。新迁移实际应用时间UTC 2026-09-06 15:59:10。首页HTTP200，原16张业务表计数与备份一致，quick_check=ok、外键问题0；新增限速表为空，未用真实账户试错。到期查询计划使用 `idx_npd_login_limits_expiry`。
- 更新后的真实D1/R2隔离备份恢复测试通过：项目和人员保留、附件逐字节一致、旧会话撤销，新增限速计数与迁移记录保留，恢复后重跑迁移不重建限速表。

目标仍未完成完整验收。后续重点包括原生Word/完整附件归档、升级时冻结运行版本与静态资源、账户编辑更精确的版本号，以及全面业务/浏览器验收；剩余上游安全告警也不能忽略。

## 2026-09-06 账户完善与本地稳定运行（阶段记录）

- 本地新建账户的初始密码改为必填，客户端提示与服务端校验一致；其他账户仍只能由管理员创建。保留旧账户和项目，不重新开放首次管理员设置。
- `tests/local-production-runtime.mjs` 改为使用实际日常入口 `scripts/local-server.mjs`：从独立空库初始化管理员，创建销售/设计/工艺/采购/生产/试验/质量七类账户并逐一登录；所有普通账户尝试创建管理员均被拒绝，无密码建员不留下记录。管理员重置密码及停用账户后，旧Cookie失效，停用人员不能重新登录。
- 同一测试验证CSS/JS资源、错误密码、登录/退出、伪造身份、不同及缺失Origin、矛盾Fetch Metadata、拒绝操作后数据计数不变、正常上传与附件内容下载、未登录访问拒绝，以及项目/概览XLSX下载。全部通过。此处是HTTP测试，不是浏览器点击验收。
- Cookie写操作在Worker入口及关键API校验同源，防止异常请求在业务写入前被框架当作其他类型请求处理。测试中 Wrangler 开发代理连续请求发生过 `Network connection lost`；同一构建直接使用Miniflare运行通过。日常入口已采用后者，不再热重载重建过程中的不完整目录。不能据此推断已定位上游内部根因或已修复Wrangler本身。
- 常规完整 `npm test`、TypeScript和改动文件lint通过，真实D1/R2备份恢复隔离测试通过。节点变更、并发冲突、历史比较、下游复核等证据沿用下方专门章节，不代表所有复杂业务情况均无缺陷。
- 切换前备份 `backups/hengda-2026-09-06T15-20-19-309Z-FW6X1N` 校验成功。首次自动重新加载服务失败，核实服务不存在后手动重新加载同一LaunchAgent成功，未修改服务目标或数据位置。首页HTTP200；原16张表与这份备份计数一致（8账户、4项目、7规格、40阶段、40版本、9审计记录），quick_check=ok、外键问题0。真实附件记录仍为0，附件结论来自隔离测试。

本次保留的限制：未进行整机重启/浏览器全面验收；原生Word及附件原件打包、登录失败限速、超大规模并发性能等仍需后续完善。下面的早期记录若与本节冲突，以本节为准。

### 本次依赖安全检查

2026-09-06 15:25 UTC 联网执行 `npm run security:audit`，包含开发/间接依赖：6项包级告警（4 moderate、2 high、0 critical），并非0漏洞。此前21项是升级前结果。React/RSC已更新至19.2.8，本地工具链与锁文件同步升级；Miniflare显式固定版本，并对其undici使用7.29.0覆盖。

- 4项中等级来自drizzle-kit下的旧esbuild开发服务器调用链；审计建议的drizzle-kit0.18.1属于倒退，未强制应用。当前日常服务不使用该开发服务器。[公告](https://github.com/advisories/GHSA-67mh-4wv8-2f99)
- 2项高级来自vinext及其image-size解析链；审计建议跳到vinext1.0.0-beta.9，未在这轮切换测试版框架。已检索框架源码：相关image-size调用用于构建期静态图片/元数据读取，当前编译后的server JS中未匹配到该解析代码，业务附件以字节上传下载。这是当前使用路径的有限检查，不是全程序不可达证明，仍需跟踪修复。[ICNS公告](https://github.com/advisories/GHSA-w3rx-r6r6-pgpr)、[JXL/HEIF公告](https://github.com/advisories/GHSA-5p2g-fcmc-qvqq)
- `scripts/audit-dependencies.mjs` 检查审计输出结构与计数，网络或解析失败退出为失败；测试覆盖失败不能被误报为无告警。未对业务文件进行对外上传，也未发布本站。

## 2026-09-06 原生 Excel 导出与完整日志

- Excel 两个下载入口改为标准 `.xlsx` 二进制及对应 MIME，采用 `write-excel-file@4.1.1` 的通用 Blob 输出路径；不再将 SpreadsheetML 文本命名为 `.xls`。长项目名的文件扩展名固定保留。
- 单项目导出14页，概览导出4页。进度是数值百分比，数量/金额为数值，业务日期为日期，UTC记录时间转换为北京时间的Excel日期时间。冻结表头、换行和列宽已设置。字段以显式字符串写入，`=HYPERLINK(...)` 等用户文字不会成为公式。
- 项目归档不再继承首页全局最近300条日志限制；在项目访问校验后，按项目查询全部日志和版本快照。新增无损分段的版本JSON快照页，保留旧表单额外字段、0、false、中文和多行文本。缺失快照明确标记，不能据此认定历史数据为空；快照不是原始附件。
- `test:export` 覆盖351条历史测试日志、越权拒绝、长Unicode快照拼接、二进制结构和公式注入防护。完整常规测试、类型和定向lint通过；独立正式Worker的HTTP测试实际建员/建客/建项目并下载项目与概览XLSX，未登录下载失败。
- 以合成数据样本使用Artifact Tool重新导入，检查14页项目文件及4页概览文件的前3行视图；独立openpyxl只读验证数值/日期/北京时间/字符串类型及全部表读取成功。该读取工具提示库未定义默认Normal样式并采用默认样式，显式单元格样式和渲染正常。不是Excel桌面程序点击验收，也不是原始表单版式复刻。
- 限制：附件仍是索引，Word仍为HTML兼容格式；概览导出仍是权限内全量数据（导出说明已注明，不应用页面筛选）。超长普通单元格会明确报错而不静默截断。跨表导出期间有并发业务修改时的一致时点快照、超大项目性能尚需完善。
- 依赖安装报告21项告警（5 moderate、16 high），尚未逐项区分运行时/开发依赖可达性；没有运行可能引入破坏性升级的自动强制修复，不能据此声明安全审计完成。

## 2026-09-06 本机日常服务切换

- 切换前完成托管离线备份 `backups/hengda-2026-09-06T12-42-01-142Z-iu9NOe`，原服务配置另存 `backups/service-before-20260906-1242.plist`。
- LaunchAgent 改为 `npm run start:local`，保留原工作目录和持久化目录。发现并修正重复端口参数导致的启动失败；托管备份校验同步支持无附加参数的正式本地启动命令。
- 服务日志 Ready on 127.0.0.1:3011，独立只读 HTTP 检查返回200。真实数据库16张表计数与切换前备份全部一致（8账户、4项目、7规格、40节点、40版本）；quick_check=ok，外键问题0。未清空、恢复或录入真实业务数据。
- 备份回归测试通过。当前验证不包括整机重启、浏览器逐项点击、全部附件实物或所有复杂场景验收。

## 2026-09-06 本地正式构建身份验证

- 身份路径增加显式 `NPD_AUTH_MODE`（local/chatgpt），无效值拒绝运行；本地构建绑定固定 local，不再因生产 NODE_ENV 改用 ChatGPT 登录。未指定标志时保留旧环境兼容分支。
- 新增 `start:local`，用 Wrangler 本地运行已经编译的 Worker，只监听127.0.0.1:3011并指向项目内持久化目录。`npm start` 不作为本项目 D1/R2 本地启动入口。LaunchAgent 的后续切换结果见上节，不能与保活服务同时启动。
- `tests/local-production-runtime.mjs` 实际启动 dist/server/wrangler.json 对应构建，使用随机端口/独立临时目录，HTTP 验证空白初始化页、首次管理员、Cookie 工作区、伪造平台头拒绝、GET退出拒绝、跨站退出拒绝且会话保持、同源POST退出和旧会话拒绝；随后错误密码无Cookie、正确密码重登成功。临时服务测试后结束，不操作3011真实数据。
- 初次验证及包含密码重登的最终复测均通过；一次追加请求审核超时未执行，重试后成功。常规完整测试、构建、类型/定向 lint 通过。
- 此处是正式构建在本地 Workerd 上的登录链路证明，不等同已切换日常服务、OS重启演练、生产数据库迁移或完整业务/浏览器验收。临时测试目录仅含合成账号，未删除现有目录或业务库。

## 2026-09-06 默认空白部署补充

- 空库默认不创建演示项目/账户/客户/订单；只有显式 `NPD_DEMO_DATA=1` 且项目和用户表均为空时才启用演示夹具。现有数据未清理或覆盖，不以删除 `.wrangler/state` 实现切换。
- 旧工艺/采购角色补齐仅在确有旧演示项目时运行，不再向全新公司数据库插入张伟、孙悦等演示人员。历史阶段基线补齐保留，不能据此宣称生产迁移机制已完成重构。
- `tests/clean-deployment.mjs` 从无演示库开始，验证六类核心表为空、并发首次管理员只有一位成功、初始化后误开演示标志也不播种；再创建七类岗位、首位客户、订单和项目，重新加载后核对8账户/1客户/1项目/10阶段/10版本及角色访问。测试“重新加载”是重新构建服务模块并读取同一隔离数据库，不等同整机重启验收。
- 内存适配器与独立非持久化 D1 的空白部署测试通过；已加入常规测试命令。README 更新首次录入顺序。当前真实数据库的既有演示/业务数据未被测试操作。
- 本地生产启动模式、正式数据库迁移、备份异地保留、完整浏览器操作和文件导出验证仍待完成，不能据此宣称已达到全系统上线验收。

## 2026-09-06 质量检验证据统计统一

- 质量阶段放行、检验页面齐套摘要、概览整机/零部件齐套数共用 `inspectionEvidenceIssues`，取当前设计版次与准确单一对象的最新提交。缺附件、不合格、缺让步依据、检验要求不一致均不计齐套。
- 零部件分母改为全部零部件，与服务端放行一致；尚未配置检验要求的零件不再从统计中消失，而是显示待补依据。
- 质量页列出每个对象缺失项，并区分当前版最新提交、同版已被后续记录替代、历史版。证据齐套仍需阶段复核，不等同已放行。
- 新增规则测试覆盖新不合格不能被旧合格代替、单一对象/项目/版次匹配、附件阶段、让步依据和缺失要求；实际 D1 用例确认较早检验日期的新提交仍排首位。独立非持久化 D1 事务回归通过。旧静态契约检查改为核对共用规则调用和规则模块中的附件校验文案，不再要求文案留在原文件。
- 仍需验证实际附件对象存在、正式检验要求的完备性及浏览器交互；本项不构成整套系统的最终验收。

## 2026-09-06 试验证据显示与放行规则统一

- 修复试验页面仅取一份当前版报告判定“已覆盖”的错误，以及概览只数有报告电机的问题。现在试验页面、概览齐套数与服务端放行共用 `testEvidenceIssues`：逐报告类型取当前设计版次最新提交，检验结果、有效附件、条件合格处置依据和试验要求一致性。
- 工作区试验/质量记录按创建时间、插入顺序倒序，与服务端判断一致；不再按用户填报的试验/检验日期决定新旧。实际补测较早试验日期的新提交排在首位。
- 显示“证据齐套·待阶段复核”而非暗示已放行；缺失项逐条提示。每份试验报告分别展示试验依据、结论、提交时间和缺附件提醒，不再用结论替代依据。
- 新增三个共用规则测试覆盖不同类型合格不能掩盖不合格、旧版排除、附件错项目/错阶段、要求不符、处置依据缺失及同类型复测覆盖。构建、类型/定向 lint、静态/单元/业务测试通过；事务排序断言修正返回对象取值后，内存及独立非持久化 D1 回归通过。
- 本轮不代表质量概览齐套统计已统一（该部分仍需复核），也不能证明已列举所有应做试验类型或真实附件对象存在；未执行浏览器交互验收。

## 2026-09-06 受控表单只读查看补充

- 原表单卡片只有有权限人员的编辑入口，其他可访问项目的人员无法直接查看当前表单内容。本轮为所有可见项目的表单增加独立只读窗口，不传入写操作；暂停项目也可查看。
- 展示完整定义字段、草稿/正式提交状态、保存版本、最后维护人及时间；未填写字段、零值、否值、多选值分别显示。额外历史字段单独保留展示，不静默丢弃。文字按文本转义，保留多行和长内容换行。
- 编辑窗口也修正数字 0 被 `||` 当作空值的问题。
- 新增实际 React 静态渲染测试，逐一渲染十份表单并断言全部定义字段出现、没有输入/保存控件；验证多行、零值、否值、额外字段和 HTML 转义。完整测试、类型检查及构建通过。此证据属于服务端渲染，不代表浏览器点击、键盘或布局验收。

## 2026-09-06 账户编辑窗口基线补充

- 账户维护提交窗口打开时的邮箱、姓名、角色、部门、启用状态及更新时间；提交时与数据库对照。其他管理员已改动可见资料或权限时拒绝旧窗口，保留输入，不产生审计或会话撤销。
- 事务内旧字段/密码哈希核对、最后管理员保护和账户变更引起的会话撤销继续保留。新增测试实际先修改部门，再用旧窗口修改姓名，断言整个账户/会话/审计状态不变。
- 基线尚非独立单调版本号：同秒仅改密码但全部可见字段不变，以及资料反复 A→B→A，仍需更强的版本标识覆盖；不应据此宣称全部账户 ABA 场景已解决。

## 2026-09-06 历史版本查看与比较补充

- 阶段版本列表新增“查看版本内容”。受权限保护的新只读接口按需读取快照并禁止缓存，不向首页批量返回所有历史快照。非项目成员拒绝读取；切换阶段会关闭原阶段的详情状态。
- 可与同阶段较早版本比较状态、完成度、计划日期、操作人/时间，以及表单、规格、零部件、试验、检验和附件索引的新增/移除/修改记录。记录前后内容可展开查看，常用字段提供中文名称；部分内部字段仍以原字段名呈现。
- 旧版本缺失领域快照、数据不完整或存在重复记录标识时明确说明不可比较，不把缺失当作删除。表单 JSON 内容先解析再稳定比较，避免仅因键顺序或空白变化误报差异。版本/时间字段的真实变化仍计入记录修改。
- `tests/revision-view.test.mjs` 三项算法测试、完整 `npm test` 通过；新版本读取/非成员隔离用例在内存和独立非持久化 D1 运行时通过。新增页面编译及类型检查通过，不代表已经浏览器交互验收。
- 本功能只读，不自动回滚/合并当前数据，不等同完整开发程序归档；历史业务主数据不足、导出原表单版式、附件保留等仍需继续验收。

## 2026-09-06 销售订单录入补充

- 新建订单与审计同批保存，事务内重新核对销售/管理员身份和不区分大小写的订单号唯一性。失败不残留订单或审计。
- 数量必须为正整数；金额为有限、非负、可安全表示到分的数值，最多两位小数，保存时消除普通浮点尾差；只接受界面支持的 CNY/USD/EUR。不再把负数量、负金额静默改成默认值。
- 订单关联窗口携带打开时的原项目 ID，原关联变化后拒绝旧窗口保存；未发生变化时不重复记录。新选择排除暂停/完成/终止项目，但保留当前关联显示，避免当前选择丢失。
- 订单录入/关联窗口保留错误提示与填写内容并防重复点击。独立 D1 运行时及内存测试通过订单号大小写并发、非法数值/币种、插入故障回滚审计和过期关联拒绝；构建、类型及定向 ESLint 通过。普通小数尾差归一化另增断言复测。
- 订单主数据后续修改、关联解除原因、关联反复 A→B→A 的独立版本号及跨请求幂等仍待完善，不代表订单全生命周期完成验收。

## 2026-09-06 真实客户录入入口补充

- 原系统仅提供演示客户下拉选择，缺少客户维护入口。本轮增加“客户资料”页及新增/修改窗口，销售和管理员可维护编号、名称、行业、联系人、电话，支持全局搜索框过滤。新客户即时进入订单/项目客户选项。
- 服务端强制销售/管理员权限并在事务中再次核对；编号大写规范化且不区分大小写防重复。修改必须填写原因、携带窗口打开时的完整旧字段，旧窗口及并发竞争失败不覆盖最新内容。
- 客户变更与包含前后字段值的审计同批保存，失败完整回滚。未提供删除入口，不会破坏已有项目/订单关联；客户名称修改会影响当前关联页面显示，历史归档是否使用当时主数据仍需继续完善。
- `npm test`、类型检查和定向 ESLint 通过；独立不持久化 D1 运行时验证权限拒绝、并发新增唯一、并发编辑/旧窗口冲突、故障时资料和审计回滚，以及“新客户→新订单→新项目”的实际数据关联。无真实客户或联系人被测试修改；浏览器交互尚未验收。

## 2026-09-06 建项订单选择补充

- 新建项目窗口增加按当前客户过滤的未关联订单多选区，显示订单号、产品、数量和交期；可无订单立项。切换客户清空订单选择，提交使用实际所选订单 IDs，不再固定空数组。
- 当工作区刷新后已选订单失效时，保留其他输入并提供移除失效选择按钮；提交前再次检查可选列表，服务端保留事务内订单归属与客户校验。
- 业务测试新增一个项目同时关联两张订单，以及跨客户订单拒绝且不改变原订单。界面已编译和类型检查，未进行浏览器点击验收；不能把业务层测试当作交互层证明。
- 上一节新建基线的内存快照比较修正后，完整 `npm test` 已通过；编号、并发建项及基线快照的独立 D1 运行时检查也已通过。

## 2026-09-06 新建项目一致性补充

- 建项原已使用批事务，本轮补齐事务内校验：创建者、负责人及五类跨部门责任人的启用状态/角色，以及订单存在、同客户、尚未归属其他项目。订单被其他项目抢先占用时整批拒绝，不再静默跳过订单关联。
- 项目编号在 INSERT 内按本年度数字最大序号分配；并发建项各获独立编号，超过 999 后仍按数字递增，不再依赖字符串排序。新建时型号不区分大小写去重，数量必须为正整数。
- 新建阶段的首个版本保存当时实际领域数据快照，不再只有“首次创建”标记。已有历史基线未改写。新建窗口失败时保留输入和错误提示。
- 隔离测试覆盖两个项目并发成功/编号不同、多规格及十阶段/十基线齐全、999→1000→1001、同订单双建项仅一成功、非法型号/数量拒绝、末步故障回滚项目/成员/规格/阶段/版本及订单关联、初始电机快照与数据库逐字段一致。独立不持久化 D1 测试通过；内存适配器的快照比较统一了 SQLite 行对象原型后复测。
- 尚未加入跨窗口/网络重试幂等键；初始快照不包含全部项目主数据/成员历史，不能据此宣称完整归档已验收。新建表单仍需补齐创建时直接选择关联订单的界面。

## 2026-09-06 暂停恢复与成员维护补充

- 暂停项目的阶段/规格/零部件/报告/附件/成员及订单关联入口拒绝写入，包括管理员；仍可读取导出。阶段事务也在最后提交处排除暂停状态，避免先校验后暂停的竞态。概览隐藏对应编辑入口并显示暂停提示。
- 项目状态变更提交窗口基线状态，所有状态变更（含恢复）必须说明原因；拒绝无效状态、重复状态与过期窗口。负责人、发起人或管理员可恢复暂停项目。既有“完成/终止项目仅管理员可修改”策略未在本轮重构，尚需设计明确重开流程。
- 状态变更、成员职责维护、订单关联更新分别与审计在同一批事务提交。成员审计保存原职责和新职责；保存时重新核对操作者、目标成员、项目状态及原成员记录。成员窗口的跨长时间编辑基线尚需补齐，当前并发保护覆盖服务端校验至提交区间。
- 新增测试覆盖无效状态、无原因、双暂停仅一成功、旧状态窗口拒绝、暂停时多个业务入口零写入、恢复成功、成员双提交与职责前后记录、状态/成员/订单关联更新失败审计回滚。内存适配器与独立不持久化 D1 运行时均通过；构建、类型及定向 ESLint 通过。本地首页复查返回 200，未修改真实项目数据。
- 尚不能声称项目创建、订单创建、所有身份/成员变更与其他业务写入之间都已具备完整事务/幂等保证；完整浏览器验收仍待完成。

## 2026-09-06 账户操作可靠性补充

- 创建/维护账户的审计与领域变更同批提交，密码、邮箱、角色和停用引起的旧会话撤销也在同一事务中。保存瞬间重新检查操作者仍为启用管理员，以及目标账户未在本次服务端校验期间被修改。
- 最后管理员保护移入事务内；两个管理员同时将自己降权时只能有一个成功。已降权的旧身份对象无法再创建账户。注意：此保护不是账户编辑窗口的完整乐观锁，打开很久的表单仍需进一步加入客户端基线版本。
- 人员分配、项目状态、新建账户、维护账户窗口补充持久错误提示及重复点击保护，失败保留输入，不再抛出未处理的提交异常。人员/项目状态服务端事务性尚待迁移。
- 本地退出改为同源 POST，GET 返回 405，外站或缺失 Origin 返回 403；本地无会话 POST 返回 303。实际无凭据 HTTP 检查得到 405/403/303，未影响真实会话。
- `npm test`、TypeScript、定向 ESLint 通过；扩展账户并发/故障/会话测试在隔离内存适配器和独立不持久化 D1 运行时均通过。测试未修改真实账户。尚未完成完整浏览器验收、登录限速和全部 HTTP 安全回归。

## 2026-09-06 报告和附件事务补充

- 试验报告、质量检验记录的领域数据、附件关联、审计、阶段版本及聚合进度已纳入同一事务；附件索引新增也纳入阶段事务。R2 对象上传与数据库不是跨系统事务，仍由上传接口在失败时清理对象。
- 报告提交携带打开窗口时的设计版次，设计已变更时拒绝旧版提交。同项目试验报告编号不区分大小写防重复，同一附件不可再次关联另一条报告。
- 提交窗口保留错误提示和填写内容；同一窗口重试复用已成功上传的同一附件。上传成功但刷新失败不再误报上传失败。尚未实现跨窗口请求幂等键，无附件检验记录的顺序重复提交仍需进一步处理。
- 类型检查、生产构建、现有集成/备份测试、定向 ESLint 均通过。新增报告用例已在内存适配器和独立不持久化 D1 运行时通过：过期版次零写入、报告/检验双提交仅一成功、末步故障回滚报告及附件关联/索引、重复编号拦截。附件测试为索引夹具，不代表 R2 上传链路或浏览器已验收。
- 本地首页 `http://localhost:3011/` 返回 200。项目/订单/成员维护的事务性、全流程浏览器验收等仍待完成；下方旧记录中报告迁移待办以本节为准。

## 2026-09-06 新增与生产确认事务补充

- `addProjectMotor`、`addPartItem`、`confirmPartItem` 已接入同一阶段事务，领域记录、审计、版本、下游复核和聚合进度一起提交。新增内容的阶段不再出现“未开始但已有进度”的不一致状态。
- 同项目同规格范围内的零部件编号按不区分大小写核对，范围包括 `motor_id IS NULL` 的系列通用件；新增和改号均受约束，修复 SQLite 普通唯一索引允许多个 NULL 导致的通用件重复问题。未自动删除已有重复数据。
- 生产确认必须提交打开窗口时的设计版次和零部件阶段版次。设计变更后，旧版生产确认不得覆盖新版；“受阻”必须填写原因。
- 扩展后的 `npm run test:transactions-runtime` 已于本轮在不持久化真实 D1 环境执行通过，覆盖编辑/新增并发、通用件重复拦截、生产重复确认、旧设计确认拒绝、末步故障回滚和快照一致性。上一轮因审核超时未执行的电机/零部件用例也已在本次获得真实运行时证明。
- 还需迁移和验收报告/检验提交、附件上传、项目及订单/成员操作；不能据此宣称全系统已经完全原子化。

## 2026-09-06 并发编辑与事务保存补充

已把受控表单、阶段状态、电机规格/设计要求、零部件修改接入 `db/revision-transaction.ts`：

- 客户端提交打开编辑窗口时的版本号，服务端再次核对；旧版或并发竞争失败返回 409，而不是覆盖最新内容。
- 以上修改的领域数据、操作记录、阶段版本、下游待复核状态和项目聚合进度在一个 D1 `batch` 事务内提交；事务中的版本快照直接读取实际更新后的数据库内容。
- 提交前校验的全项目阶段版本/状态、项目状态、账户角色/启用状态会在事务内复核；先验证后发生的已记录变更也会导致回滚。
- 编辑窗口保留冲突时的填写内容并显示持久错误提示；刷新失败与保存失败分别反馈，不自动重复发送业务修改。
- `tests/revision-transactions.mjs` 验证同版表单/阶段/电机/零部件双提交仅一个成功、过期版本零写入、最终聚合更新处故障使整个动作回滚、快照内容一致，以及失败不会误清空零部件原生产确认。
- 表单与阶段的并发、回滚检查已通过 `tests/revision-transactions-runtime.mjs` 在不持久化的真实本地 D1 运行时执行；不会读写业务库。后续电机/零部件真实运行时用例最初两次因审核超时未执行，已在上方“新增与生产确认事务补充”记录的本轮复测中补齐。浏览器交互层尚未完整验收。

**仍不能宣称所有操作都已原子化。** 本节初次检查时新增规格/零部件、生产确认尚未迁移，现已在上方补充中完成；报告/检验提交、附件上传、订单/成员维护等路径仍需迁移和测试，其中有的业务写入仍发生在阶段事务之前，跨旧新写入路径的并发完整性尚未获得证明。并发保护也不等同于支持自动合并或版本回滚。

## 2026-09-06 本地备份与恢复验收补充

本项取得实际恢复证据，但**系统整体仍未验收完成**。

- `scripts/local-backup.mjs` 提供离线备份、清单校验、新目录恢复；包含 D1 数据库与 R2 文件，不覆盖已有数据，恢复后撤销旧本地会话。
- `tests/local-backup.test.mjs` 验证运行中数据库拒绝备份、记录恢复、二进制文件恢复、哈希损坏、路径越界、符号链接、现有目录覆盖保护和会话撤销。
- `tests/local-backup-runtime.mjs` 在独立临时目录使用真实 Miniflare D1/R2 写入项目、人员及附件，备份后以新目录启动，确认记录与附件逐字节一致。已实际执行通过。
- 真实业务备份 `backups/hengda-2026-09-05T14-19-54-218Z-rd3AeX` 已校验并恢复至 `restore-drills/2026-09-05-1420`：8 个账户、4 个项目、40 个 Sheet，恢复副本撤销 1 个旧会话；未替换当前业务目录。
- 2026-09-06 执行 `npm run backup:local:managed` 成功，生成 `backups/hengda-2026-09-06T01-48-51-792Z-cyeO9e`。该命令识别 `com.hengda.npd.local` 的项目归属，暂停自动保活服务、等待数据库句柄释放、备份后重启；失败路径也曾实际触发并恢复服务。结束后 `http://localhost:3011/` 返回 200。
- 当前真实库的 `npd_documents` 数量为 0，因此不能声称恢复过真实业务附件；附件恢复结论来自上述隔离运行时测试。
- 操作说明见 `docs/local-backup-recovery.md`。备份未加密、尚未复制到另一块磁盘，也未配置自动备份任务。更换存储运行时版本后需再次演练。

## 2026-09-05 当前复核结论

**目标仍在进行中，尚不能宣称已完成正式使用验收。** 下方原有表格是早期 V2 检查记录，不应作为当前版本全部能力已验收的证据，尤其不能把源代码契约测试视为浏览器测试，或把导出接口返回 200 视为文件格式与版式已验证。

本轮新增可复现证据（`tests/store-integration.mjs`）：

- 同时进行两次首次管理员初始化，仅一次成功；失败请求不能覆盖胜出账户密码。初始化标记阻止以后重新开放首次设置。
- 本地请求只验证本地会话，不使用可伪造的身份请求头；密码重置后旧会话失效（请求层绕过修复目前经源码核对，仍需 HTTP 安全回归）。
- 设计变更使已完成下游阶段进入待复核；版本保存业务快照与修改前数据。演示成员修复不再给实际项目自动加成员。
- 试验按设计版次、报告类型分别取最新提交；型式试验不合格不能被性能试验合格掩盖。缺附件不计入齐套进度，不允许阶段完成。
- 质量检验需要当前版次的整机及零部件有效记录与附件；不合格、缺附件阻止放行。报告要求不能脱离设计输出，检验记录不能同时关联整机与零部件。

正式使用仍需完成的验收项：

1. 在隔离的真实本地运行环境执行完整登录、账户维护、上传下载、导出和反复变更流程；对生产数据不做试写。
2. 本地运行与迁移方式标准化，清晰区分演示种子数据和正式空库；验证重启与升级不会污染真实数据。
3. 数据库与附件的成套备份、恢复演练和操作说明。Excel/Word 导出不是数据库备份。
4. 并发编辑冲突、重复提交及领域数据/审计/版本写入的原子性；目前不能声称所有写入均具备事务保证。
5. 版本内容在界面中的查看/比较与完整归档。历史升级基线只有部分元信息，不能还原未保存内容。
6. Excel已改原生XLSX（验证范围见2026-09-06补充）；Word仍为HTML兼容格式，原表单版式和附件原件打包未完成。
7. 八角色全流程权限、负责人交接、整机节点确认及暂停/终止后操作的逐项检查。
8. 登录失败限速、跨站请求、会话过期提示及真实运行时密码计算兼容性验证。

## 早期 V2 检查记录（历史，不代表当前完整验收）

审计基准：`15.产品开发控制程序.docx`、HD/JL-SJ-01A1～10A1，以及多 Sheet、六类人员、订单、进度、多规格、时间戳、质量检验和导出要求。

| 要求 | 实现证据 | 结论 |
| --- | --- | --- |
| 项目多 Sheet | `lib/sheets-v2.ts`、`ProjectWorkspace.tsx` 的概览 + 10 Sheet | 已实现 |
| 10 份原程序表单 | `lib/forms.ts` 的 SJ-01～SJ-10 字段，按 Sheet 映射 | 已实现 |
| 六类登录角色 | `lib/npd-v2.ts`、`lib/access-v2.ts`、服务端权限校验 | 已实现并越权测试 |
| 销售/设计建项 | `createNpdProject()` 限制 admin/sales/design | 已实现并测试生产越权 |
| 项目可见范围 | 管理员全部；其他为发起、负责或明确参与 | 已实现并测试非成员隔离 |
| 多规格电机 | `npd_project_motors`，建项可批量创建且型号唯一 | 已实现并测试一对多 |
| 订单关联 | `npd_sales_orders`、订单台账、同客户校验、项目多订单 | 已实现并测试建项关联 |
| 零部件节点 | 规格/通用件、图号材质、设计输出、计划日、生产确认 | 已实现并测试生产权限 |
| 试验员报告 | 按电机规格、试验要求、报告编号、结论和附件 | 已实现并测试越权拦截 |
| 质量检验 | 整机/零部件记录，检验要求由设计输出自动派生 | 已实现并验证来源一致 |
| 阶段门 | 前置 Sheet、表单、零部件、试验、质量齐套校验 | 已实现并测试绕过拦截 |
| 时间戳 | `npd_activities` 记录操作者、动作、对象、详情和服务端时间 | 已实现并测试 |
| 图形化看板 | 多周期、个人指标、状态环图、月度趋势、重点项目 | 已实现并浏览器验收 |
| Excel 导出 | 项目组合和单项目多工作表原生 XLSX | 2026-09-06 独立运行时下载、读取和权限测试通过，详见补充 |
| Sheet/整套归档 | Word 兼容单 Sheet 与完整开发程序档案 | 已实现并检查响应头 |
| 附件 | R2 上传/鉴权下载、25MB 限制、失败对象清理 | 已实现 |
| 数据迁移 | V2 使用 `npd_` 前缀，增量迁移仅建表/索引，不删除 V1 | 已核对 |
| 响应式界面 | 桌面与 390×844 手机视口、移动侧栏 | 浏览器验收通过 |

## 已执行验证

1. TypeScript 严格类型检查：0 错误。
2. ESLint：0 错误、0 警告。
3. Vinext 生产构建：主页和 6 个 API 路由成功产出。
4. 静态契约测试：3/3 通过。
5. 内存 D1 业务集成：六角色、多规格、订单、访问范围、表单、阶段门、零部件、试验、质量、时间戳和归档数据通过。
6. 本地 API：工作区健康检查通过；项目概览 Excel、项目 Excel、完整 Word、单 Sheet Word 四个导出端点均返回 200 和正确附件类型。
7. 浏览器：看板、订单台账、订单进入项目、10 Sheet、零部件、试验、质量要求自动引用、建项对话框、移动布局均通过；控制台无 error/warn。
8. 演示数据一致性：增加一次性迁移标记，校准 Sheet 状态、零部件明细和聚合进度。

## 本地运行状态

本地开发服务由 `npm run dev` 启动，终端显示实际访问地址。正式生产使用前仍需导入企业真实账号、客户、订单和在研项目，并制定 D1/R2 备份与附件保留策略。
