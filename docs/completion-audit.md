# 新品开发系统完成度审计

审计基准：`15.产品开发控制程序.docx`、HD/JL-SJ-01A1～10A1，以及用户提出的人员权限、订单关联和进度控制要求。

| 要求 | 实现证据 | 当前结论 |
| --- | --- | --- |
| 立项至定型的阶段门 | `db/store.ts` 的 9 个关口、`ProjectDrawer` 阶段视图 | 已实现并完成内存 D1 闭环测试 |
| 全新/派生产品差异 | 全新产品禁止裁剪；派生/改进产品授权裁剪并强制记录原因 | 已实现并测试 |
| 2:1:7 初始排期 | `gateScheduleRatios` 基于项目起止日生成阶段计划 | 已实现 |
| 10 份受控表单 | `lib/forms.ts`，10 个定义、135 个字段 | 已实现并完成唯一性/必填项检查 |
| SJ-02/SJ-03 策划齐套 | `requiredFormsForGate()` | 已实现，集成测试验证缺表不能放行 |
| 关口放行控制 | 前序关口、已批准表单、验证/确认/定型附件检查 | 已实现并测试 |
| 订单关联 | 订单录入、型号一致、交期约束、关联/解除关联 | 已实现并测试 |
| 进度控制 | 未开始、进行中、受阻、完成、已裁剪；计划日与前后节点约束 | 已实现并测试 |
| 问题闭环 | 建立问题、责任人、严重度、期限、解决与验证记录 | 已实现并测试 |
| 设计变更 | 申请、批准/退回、通知、旧件处置、实施验证归档 | 已实现并测试 |
| 人员权限 | 12 类岗位、18 项原子权限、服务端二次校验、账号启停 | 已实现；权限不变量检查通过 |
| 审批隔离 | 审批角色匹配，系统管理员可代管，其他角色不能越权 | 已实现 |
| 项目状态 | 暂停、恢复、定型完成、书面原因终止 | 已实现并测试 |
| 文件与证据 | R2 文件上传/下载、25MB 限制、表单关联、证据计数 | 已实现；生产 R2 由 Sites 绑定 |
| 数据持久化 | D1 12 张表、7 个索引、外键完整性、自动初始化与演示数据 | 迁移已生成，73 条运行 SQL 预编译及闭环集成测试通过 |
| 身份与匿名访问 | ChatGPT 工作区身份；生产匿名用户强制只读；首位登录管理员 | 已实现 |
| 响应式工作台 | 项目、订单、表单、审批、变更、报表、权限七类业务视图 | 生产构建通过；本机安全策略禁止 localhost 浏览器预览 |
| 构建与发布 | Vinext/Cloudflare/Sites 配置 | TypeScript、ESLint、生产构建及完整测试全部通过 |

## 已执行验证

1. `tsc --noEmit`：通过，0 个类型错误。
2. ESLint：通过，0 个错误和警告。
3. Vinext 生产构建：通过，主应用与 4 个 API 路由全部产出。
4. 静态契约测试：2/2 通过。
5. 表单/权限不变量：10 份表单、135 个字段、12 类角色通过。
6. Drizzle 迁移：12 张表、7 个索引及外键约束成功生成并核对。
7. 73 条数据库运行 SQL 全部成功预编译。
8. 数据闭环集成测试：订单 → 项目 → 表单 → 审批 → 关口 → 问题 → 暂停/恢复 → 变更验证 → 派生流程裁剪 → 项目终止 → 订单解绑，全流程通过。

## 运行验收说明

本机浏览器安全策略明确禁止自动化访问 `localhost`，因此未将本地浏览器截图作为发布阻断项。Sites 发布使用同一份已通过类型、规范、构建与数据闭环测试的源码；生产运行状态、D1/R2 绑定和最终访问地址由 Sites 部署结果确认。
