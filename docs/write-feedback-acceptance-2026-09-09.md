# 登录失效与保存结果提示补验 · 2026-09-09

本地启用版：`20260909T142813Z-e8caf04ca752`。只调整工作区写入、上传后的结果识别和提示；沿用本地账户及服务端权限，不修改真实账户、阶段门禁、数据库结构或依赖。

## 修复内容

- 已获服务器成功回执的操作，即使随后刷新失败，仍向表单返回成功。根据刷新原因分别提示“已保存，需重新登录”“已保存，请确认账号”“已保存，页面待更新”，不再以“未能完成”误导重复提交。附件对应显示已上传及不要重复上传。
- 写入入口明确返回401或账号上下文409时，提示“本次操作未执行”，提醒复制留存后重新登录/确认账号。保留当前页面人员、快照与未提交表单，不自动跳转或替换人员。
- 写请求断网、成功响应截断/格式异常、上传缺少文件标识或服务器5xx时，保守显示“结果待核对”。不能根据无回执认定没有写入，不自动重试；表单保持打开，提示先保留输入、核对最新记录后决定是否重试。
- 上述重要提示不设自动消失计时，提供命名关闭按钮及读屏通知；手动关闭或后续操作提示可替换它。普通成功/校验错误仍使用短时提示。新提示取消旧计时器，卸载时清理，避免旧提示把新提示提前关闭。
- 没有新增浏览器草稿持久化，刷新、关闭或退出仍可能丢失未保存内容；提示不能替代主动保存或复制。已经保存但没有最新快照时，页面可能仍显示旧值，须依提示核对。

## 验收证据

- 工作区真实回调及React控件渲染、身份/认证专项共53项通过：写入/上传成功后401、异账号、断网和无效JSON；写入前401/账号409；回执丢失/异常/503；缺文件ID；普通冲突只刷新一次；旧计时器包含ID零、手动关闭和卸载清理。调用次数、原快照/人员不被替换、已保存仍返回成功均有断言。
- 提示控件的成功status、警告/错误alert、关闭按钮名称、中文标题及内容转义经过实际组件静态渲染检查。该测试不是浏览器截图或键盘实测。
- TypeScript、范围ESLint、diff检查、完整npm test及生产构建通过。
- 封存候选完整production-runtime在全新临时数据和随机端口通过，包含八角色、日志范围、十阶段、版本/权限及三类档案。新增合成管理员自身修改：操作200 → 旧会话刷新401 → 旧会话写入拒绝 → 新密码重新登录后姓名与递增一次的版本准确保留。没有修改3011真实账户密码。
- 本轮没有新浏览器交互、Office视觉、真实断网环境或长时间运行验收；不将回调模拟与真实HTTP拼成浏览器全路径已验证的结论。

## 本地更新与数据保留

核实原LaunchAgent及固定版本后，暂停同一服务并生成/验证成套备份：`backups/hengda-2026-09-09T14-29-40-034Z-bW1OmY`。随后启用同一已验收候选并重启：60文件封存校验通过，日志版本匹配，PID61257/running，首页HTTP200。

17张npd表结构及全部原值与备份逐值一致；quick_check正常、外键0。8账户、4项目、7规格、40阶段、23表单、40条阶段历史及11条既有活动均保留。没有迁移、依赖升级、原模板更改或远程发布。

Sites错误/成功状态规范影响了三类结果区分、重要提示保留与可访问性处理。无更改归档业务规则、旧资料复核、剩余依赖风险和受控异盘备份安排继续保留；整体目标不据本轮修复判为完成。
