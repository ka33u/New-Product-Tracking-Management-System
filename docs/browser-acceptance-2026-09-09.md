# 本地补充验收 · 2026-09-09

## 第四轮：可读性、窄屏节点信息与导航

本轮仅调整页面样式和移动导航交互，未改变权限、阶段放行、数据模型、历史记录或导出内容。继续使用独立53836及既有合成库，正式3011未参与模拟操作。最终候选及本地启用版为 `20260909T134539Z-45a1dcd2016b`。

- 基线390px页面实测横向内容394px；负责人、当前阶段和进度在紧凑看板被隐藏，项目关键指标也被隐藏。电机状态标签误继承图标32px定宽，文字内容40px。按Sites可读性规范，常用字号改为至少14px等效rem，正文/表单16px，增强次要文字对比，并将状态标签与图标盒样式分离。
- 保留桌面布局，窄屏改为项目、负责人、状态、阶段责任人、百分比纵向排列；项目关键指标、阶段版本及维护人员不再隐藏。任务清单保留阶段、姓名、计划日期和状态。工具栏允许换行/滚动，阶段标签横向滚动，长表单弹窗内部滚动。
- 第一候选 `20260909T133822Z-99f71674889e` 完整npm test、TypeScript、范围ESLint及候选完整production-runtime通过。实际看板320/390/768/1280px下页面宽度与scrollWidth相等；320px自定义日期筛选换行后无页面横溢。390px项目关键指标和Sheet 6阶段版本/人员可见，报告正文16px，底部工具栏390×98；1280px电机状态标签不再截字。
- 第二候选移动导航首次实际验证发现打开后焦点仍留在按钮、Escape未关闭，因此未部署。最终实现下一帧聚焦关闭按钮、在应用根节点处理Escape、关闭后返回打开按钮，并为隐藏侧栏设置visibility及仅transform过渡。最终候选实际390px打开菜单后焦点为“关闭导航”、aria-expanded=true；Escape后焦点回到“打开导航”、expanded=false、侧栏隐藏。
- 最终候选390px任务六行均保留阶段、责任人和日期；每行宽度与scrollWidth均360px，页面390px，无横溢。320×740新建项目弹窗宽307px、高710px，正文可视632px/总高3240px，全部输入/选择/文本框16px；截图及元素边界检查未见页面外表单控件。包含工艺/采购/生产/试验/质量负责人及出线/防护/绝缘/冷却字段。未填写或提交项目，Escape关闭后焦点返回创建按钮。
- 最终候选1280×720看板宽度/scrollWidth均1280px，负责人和当前节点责任人分别显示，49%/100%与原数据一致；采集warn/error为空。模拟账户正常退出，视口恢复默认，关闭仅本轮测试标签8，正式标签未关闭。没有模拟业务保存或新增版本。
- 最终12项控件专项、TypeScript、范围ESLint、构建及完整production-runtime通过；覆盖八角色账户/会话、跨站拒绝、旧版本冲突、交接、十阶段放行和Word/Excel/ZIP等接口路径。本轮未重新生成或视觉检查办公文件，也未将窄屏视口测试等同200%文字放大或全页面全尺寸验收。

更新前核实同一LaunchAgent及旧固定版本，暂停后生成并验证 `backups/hengda-2026-09-09T13-54-34-549Z-EbuUr3`，再启用最终候选并恢复服务。17张npd表结构及所有字段与备份逐值相同，quick_check=ok、外键错误0；保留8账户、4项目、23表单、40阶段和40历史等全部数据。60文件封存校验通过，运行日志匹配新版本，PID53579/running，localhost:3011 HTTP200。独立验收PID52472精确停止，合成库保留。无迁移、依赖、原模板或真实账号/项目资料修改，无远程发布。

仍保留无更改归档规则待确认、23份旧表单业务复核、依赖告警和异盘备份安排；本轮不据可读性补强宣称全部正式业务验收或零bug。

## 第三轮：只读权限入口及已归档单规格改版后重新放行

待更新候选 `20260908T190237Z-a8d9b71388f2`，独立53836/合成库 `/private/tmp/hengda-browser-qa-LvSvkr`。变更前生成并校验模拟库备份 `hengda-2026-09-08T19-02-49-609Z-mUIMwW`，正式3011及真实业务数据未参与测试。

- 修复概览入口与服务端锁定不一致：暂停时隐藏新增规格、设计输出编辑和成员维护；已完成/终止项目的普通人员只读，隐藏无权限的状态及交接入口。保留管理员处理已关闭项目、暂停项目授权负责人恢复和交接的既有能力，未扩大服务端权限。增加清晰只读/恢复提示，导出与历史仍可用。
- 新增状态/角色矩阵回归，旧实现先复现暂停项目暴露入口后修复；9项控件测试、TypeScript、范围ESLint、完整npm test及封存候选完整production-runtime通过。候选60文件校验通过，没有依赖或迁移修改。
- 实际浏览器由设计账户查看已完成NP-2026-002，确认只读提示及维护入口隐藏。管理员在已归档项目仅把132S出线改为右侧并升级R2，系统撤销下游放行、清除该规格当前生产确认，项目自动重新进入进行中。160M和共用件R1生产、试验和检验保留。
- 设计、生产、试验、质量、销售账户逐次退出再登录，实际逐字段提交SJ04/05/06/07/08/09的V2，记录原因；原任务书与计划经模拟适用性复核保留V1。生产重新确认132S R2；试验和质量各新增一条R2记录及一个合成附件。旧132S R1报告明确显示历史版，不计入当前证据；160M和共用件R1仍计入齐套。
- 逐阶段重新复核后十阶段全部100%，项目completed、实际完成2026-09-09、生命周期V4。看板当前责任节点随设计、生产、试验、质量、销售变化；最终普通设计账户再次回到只读状态。
- 与变更前模拟备份逐字段对比：旧45条阶段历史、2条试验、3条检验、5条附件元数据及共用件全部不变；160M整行不变。现在150条阶段历史、3条试验、4条检验、7条附件；外键检查0。132S R2当前完工日期2026-09-09，由模拟生产账户确认。没有通过接口预填本轮浏览器业务步骤。
- 浏览器普通点击实际保存 `/Users/zhangyu/Downloads/NP-2026-002_QA-连续十阶段验收-0908_完整离线档案 (2).zip`，310708字节、生成2026-09-08T19:23:33.382Z、文件修改19:23:33.691Z。CRC通过，旧5份和新2份原件分别逐字节等于对应合成源文件。包含Word/Excel，但不将ZIP结构验证等同办公软件视觉验收。
- 归档十阶段版本3/7/8/9/14/19/24/20/22/24；附件自身A1与关联设计R1/R2是不同字段，不混同。新模拟更改单明确技术R2变更，未实施尚待企业确认的“无更改声明”归档路径。
- 最终1280×720概览截图核对，浏览器warn/error为空；模拟账户正常退出，临时标签7关闭，用户正式标签未关闭。连接自动批准两次出现超时，各在一次获准重试后继续，没有绕过浏览器权限。

正式部署：核实LaunchAgent配置及旧版本后暂停同一服务，生成并验证 `backups/hengda-2026-09-08T19-27-53-547Z-LZcPoW`，启用已验收候选 `20260908T190237Z-a8d9b71388f2`，随后恢复托管服务。17张npd表结构和所有字段与备份逐值一致，保留8账户、4项目、23表单、40阶段及40历史记录等全部原数据；quick_check正常、外键0。启动日志匹配新版本，3011首页HTTP200。没有迁移、依赖变更或真实账号/业务修改。精确停止独立验收PID54526，保留模拟库和下载文件。最终TypeScript、范围ESLint及diff检查通过。

本轮合成数据不代表真实客户签字、试验/质量合格、技术更改或量产批准。原模板不变。无更改归档审批方案、旧资料业务复核、其他未测复杂组合、办公软件视觉及长期运行保管边界仍保留，不声称零bug或整体正式业务验收全部完成。

收尾只读核验：正式服务running/PID59104，localhost:3011首页HTTP200；独立验收会话退出143、无额外错误输出。执行前进程检查的自动批准曾超时未运行，获准重试一次后完成，未视为应用故障或绕过审批。

## 第二轮：阶段提示对齐、直接ZIP下载及本地更新

待更新版及最终本地版 `20260908T184720Z-26c0d604ba41`。本轮使用原53836合成库，没有提交业务修改，也未实施尚待确认的无更改归档规则。

- 试验/质量面板同时判断当前证据覆盖和该项目对应阶段状态：齐套且阶段completed显示“证据齐套 · 阶段已完成”；进行中/待确认仍提示待复核，受阻不被齐套掩盖。旧完成状态但当前证据缺项显示“证据待补齐 · 完成状态需复核”。未知/缺失阶段不推断完成，零目标不等同齐套。此函数仅用于展示，未修改放行门禁或汇总进度。
- 已保存阶段备注标注“阶段备注（记录时内容）”，原文字段逐字保留，包括旧的未来式提示。没有把历史描述替换成未经记录的批准结论。
- ZIP链接去掉新窗口target，与既有Excel/Word保持原生直接下载，保留未加密警示。没有前端整包缓存、绕过鉴权、自动重试或改变流式归档实现。
- 13项证据/控件专项、TypeScript、范围ESLint、diff及完整npm test通过；封存候选完整production-runtime通过，涵盖账户、八角色、同源保护、版本/交接、附件及十阶段HTTP放行、Word/Excel/ZIP。
- 实际浏览器核对NP-2026-002：普通点击ZIP首次触发download，不再依赖downloadMedia；Sheet 6的两规格及Sheet 7的3/3证据均显示阶段已完成，原备注和V7/V9保留。NP-2026-001改R2后仍显示证据待补齐及历史R1报告，不误显完成。当前1280×720截图核对既有布局；未宣称全尺寸验收。
- 对浏览器实际保存的 `/Users/zhangyu/Downloads/NP-2026-002_QA-连续十阶段验收-0908_完整离线档案 (1).zip` 只读验证：生成时间2026-09-08T18:50:13.890Z、文件修改时间18:50:14.020Z、111667字节，CRC通过，十阶段版本3/4/4/3/6/7/9/3/3/3，五份附件分别与合成源文件逐字节相同。Word/Excel文件存在，但本次不检查办公软件排版。该实际文件证明本轮普通点击完成了完整ZIP交付，不只是事件。
- 同时核对前一轮文件链接下载留下的无序号ZIP：生成时间18:36:30.177Z，111672字节，CRC/五份原件也通过。这补强前一轮downloadMedia的完整交付证据；不据此反推此前target=_blank普通点击成功或给未知来源取消日志定因。两份合成下载保留，未删除用户下载文件。
- 测试浏览器warn/error为空，模拟账户退出、临时标签关闭。正式3011标签未进行业务操作。
- 停止已核实的本项目托管服务，生成并验证备份 `backups/hengda-2026-09-08T18-52-33-943Z-qwvT9y` 后激活同一候选，再恢复托管服务。17张npd表结构和所有字段与备份逐值一致，quick_check正常、外键0，60文件封存校验通过。无迁移或依赖变动。
- 更新后托管PID52048/running，启动日志匹配20260908T184720Z-26c0d604ba41，3011首页HTTP200。日志存在既有NUL字节时用文本模式限定匹配启动行，没有改写/清空日志。独立验收PID51684精确停止，会话退出143，本轮收尾无额外输出；模拟库和下载文件保留。最终diff检查通过。

本轮关闭了下文第一轮的显示不一致及ZIP普通点击完整交付缺口。无更改归档企业规则、旧资料复核、全部复杂流程组合、办公软件视觉及运行保管等仍按验收清单保留，总目标未据局部修复判定完成。

## 第一轮：解锁后补充验收（历史记录）

北京时间凌晨，Mac已解锁。使用固定版本 `20260908T154043Z-871b1614e904`、独立端口53836和既有合成库 `/private/tmp/hengda-browser-qa-LvSvkr`。正式3011标签页未进行业务操作，原控制程序和模板未改动。

## 实际浏览器结果

- 重启后看板保留2项目、1进行中、1已完成、4规格、平均进度75%；NP-2026-001为49%，NP-2026-002为100%。已完成项目十阶段均100%，负责人、最后节点及两规格的生产完成状态仍在。检查当前1280×720视口项目概览截图，标题、状态、阶段导航及团队信息可读；不代表全部尺寸验收。
- NP-2026-002的完整Excel、完整程序Word、Sheet 6 Word、指定规格试验附件、通用零部件检验附件，以及看板项目概览Excel，实际点击均收到浏览器download事件。这证明浏览器触发交付，不声称对这些下载副本做了Office视觉验收。
- ZIP普通点击后，当前标签的download等待20秒超时。DOM表明该链接使用`target="_blank"`，页面未出现错误，新标签列表没有留存弹出页；这不足以认定服务端失败或普通点击已成功。改用浏览器公开的文件链接`downloadMedia`接口，同一链接收到download事件。保留普通点击/跨窗口事件捕获边界，不修改DOM或放宽服务器导出校验。
- 设计账户在NP-2026-001打开“添加或维护项目成员”、指定型号“变更设计输出”、指定零部件“变更零部件”，对应弹窗包含正确项目、型号和R2/R3版次。检查后取消，未编辑或保存。
- 设计账户的生产确认入口不可见。退出回到登录页后，使用既有模拟生产账户登录；对应整机和零部件确认入口可见，零部件弹窗“当前状态”为中文“已完成”。取消后退出，未重新确认节点或制造新版本。
- 试验/检验下载入口包含对象、报告号或版次/日期；附件分类显示“试验报告”“检验记录”。最终模拟设计会话亦退出，临时标签关闭，采集的warn/error为空。正式标签未关闭。

## 独立HTTP内容核验

与浏览器分开建立短期模拟设计诊断会话，对NP-2026-002完整ZIP做一次读取，随后注销。没有读取浏览器Cookie或使用正式账户。

- HTTP200，application/zip，Content-Length与实收111670字节一致。
- JSZip开启CRC校验成功；包含Word、Excel、附件清单和5份独立附件原件。
- 五份原件分别与指定合成文本文件逐字节一致。同名文件没有合并丢失。
- 十阶段版本为3/4/4/3/6/7/9/3/3/3，与此前连续流程一致。
- 本次为ZIP结构、完整性和附件内容校验，不是浏览器下载文件的逐字节复核，也不是Word/Excel排版或真实技术签批验收。

## 保留事项

1. “无更改声明→管理员确认→归档”仍需用户明确确认企业审批规则，未实施或对历史资料追认。确认问题已再次提出，其他验收未因此暂停。
2. 已完成的Sheet 6/7仍有“证据齐套·待阶段复核”“齐套后仍需阶段复核”等固定提示，及此前保存的未来式原件核验说明。页面主状态和历史明确为已完成，但这些提示需要与阶段状态进一步对齐；本轮仅记录，不改写历史备注或放行结果。
3. ZIP普通点击的跨窗口交付仍需进一步直接验证；已记录的文件链接下载和独立HTTP完整性不能冒充普通点击已通过。
4. 其余范围仍见local-acceptance.md：旧业务资料、全部复杂重开组合、办公软件视觉、依赖告警和异盘备份安排未据本次消除。不宣称系统零bug或正式业务全面验收完成。

本次仅补充验收记录，没有源程序、依赖、迁移、正式业务资料或部署版本变更。临时测试库保留以便后续复验。

收尾只读核验：正式本地版本指针未变，LaunchAgent为running/PID36312，localhost:3011首页HTTP200。首次进程/健康检查因自动审批超时未执行，获准重试一次后完成，不将审批超时算作应用故障。文档diff空白检查通过。

核实独立服务PID48428后发送TERM，执行会话退出143，测试服务已停止、测试库保留。收尾日志出现一条“离线归档下载中断 Network connection lost.”，无法仅从该条定位到普通点击、文件链接下载或关闭标签引起的取消；因此浏览器ZIP仍仅记download事件，不记完整流交付。独立HTTP的完整接收和CRC证据不受该不明来源取消替代，也不掩盖这一日志。
